/**
 * This file was auto-generated by openapi-typescript.
 * Do not make direct changes to the file.
 */

export interface paths {
  "/v1.2/chat/completions": {
    parameters: {
      query?: never;
      header?: never;
      path?: never;
      cookie?: never;
    };
    get?: never;
    put?: never;
    /**
     * Create a health chat completion
     * @description **API key only.**
     *
     *     Non-streaming OpenAI Chat Completions subset: model january, text messages, and optional stream false only. Uses supplied history and clinical literature when useful, with citations in the answer. No patient account or persistent memory. Accepted transcripts are retained for debugging and evaluation. One successful completion costs one credit. Five concurrent requests per customer by default.
     */
    post: operations["createChatCompletion"];
    delete?: never;
    options?: never;
    head?: never;
    patch?: never;
    trace?: never;
  };
  "/v1.2/auth/client-tokens": {
    parameters: {
      query?: never;
      header?: never;
      path?: never;
      cookie?: never;
    };
    get?: never;
    put?: never;
    /**
     * Mint a client token
     * @description **API key only.**
     *
     *     Exchanges your partner API key for a short-lived token bound to one of your end users, so your mobile app can call the v1.2 API directly instead of through a proxy that holds your key.
     *
     *     **This endpoint requires your API key (`sk-…`), so always call it from your backend** — behind whatever login already protects your own APIs. Never ship your API key in a mobile app in order to call this from the device: that puts a credential for your whole account in every copy of your app, which is the problem client tokens exist to solve.
     *
     *     The end user is identified by `end_user_id` in the body, not the `January-End-User-ID` header: the token is bound to it, and requests made with the token act only on that user, whatever headers they carry.
     *
     *     Tokens last 300–7200 seconds and the raw value is returned exactly once — it is stored only as a hash, so it can never be retrieved again. Relay it to the device and let the device refresh when it expires: a `401` with code `token_expired` is the signal to mint a new one and retry the original request once.
     */
    post: operations["createClientToken"];
    delete?: never;
    options?: never;
    head?: never;
    patch?: never;
    trace?: never;
  };
  "/v1.2/auth/client-token-revocations": {
    parameters: {
      query?: never;
      header?: never;
      path?: never;
      cookie?: never;
    };
    get?: never;
    put?: never;
    /**
     * Revoke an end user’s client tokens
     * @description **API key only.**
     *
     *     Revokes every outstanding client token for one end user. Safe to call repeatedly: it reports how many tokens it actually stopped, so an immediate second call reports 0.
     *
     *     **This endpoint requires your API key (`sk-…`), so always call it from your backend.** A client token cannot revoke anything, its own included.
     *
     *     Revocation takes effect within 60 seconds, the authentication cache window. For an immediate cut-off, stop trusting the user in your own app as well — and note that a token minted in the same instant as the revoke may survive it, bounded by its own expiry.
     *
     *     If some tokens cannot be revoked, the call answers `503` with code `client_token_revocation_incomplete`, naming how many of them succeeded rather than reporting a smaller count as though it were the whole story — **retry until it succeeds**, which only picks up the remainder.
     *
     *     One call stops at most 500 tokens so it cannot time out, so **repeat the call until `revoked_count` is 0** if a user is somehow holding more than that (which means your app is minting per request rather than per session).
     */
    post: operations["revokeClientTokens"];
    delete?: never;
    options?: never;
    head?: never;
    patch?: never;
    trace?: never;
  };
  "/v1.2/credits": {
    parameters: {
      query?: never;
      header?: never;
      path?: never;
      cookie?: never;
    };
    /**
     * Get your credit balance
     * @description **API key only.**
     *
     *     Your API credit allowance and consumption for the current calendar month (UTC). Each successful billable data operation consumes credits — how many depends on the operation and your plan — while requests that fail cost nothing and v1.1 calls are not counted. Checking your balance, creating client tokens, and revoking client tokens never consume credits. Reading your balance is also exempt from the request limits that bound the rest of the API, so it keeps answering once your allowance is spent or your request limit is reached — it carries only a cap of its own, 60 reads per minute unless we have agreed a different one with you. Read your balance when a request is rejected or on a schedule rather than before every call, and treat that balance — not a fixed per-call price — as the source of truth. When credits run out, v1.2 endpoints return `429` with code `credit_limit_exceeded` until the allowance resets — retrying does not help before then.
     */
    get: operations["getCredits"];
    put?: never;
    post?: never;
    delete?: never;
    options?: never;
    head?: never;
    patch?: never;
    trace?: never;
  };
  "/v1.2/foods": {
    parameters: {
      query?: never;
      header?: never;
      path?: never;
      cookie?: never;
    };
    /**
     * Search foods by name
     * @description **API key or client token.**
     *
     *     Full-text search over the January food database, returning up to 40 ranked matches. Generic foods, branded products and recipes are searched together unless `type` narrows it to one. To look up a scanned barcode, use `GET /v1.2/foods/barcode/{barcode}` instead.
     *
     *     Callable with a client token carrying the `foods:read` scope.
     */
    get: operations["searchFoods"];
    put?: never;
    post?: never;
    delete?: never;
    options?: never;
    head?: never;
    patch?: never;
    trace?: never;
  };
  "/v1.2/foods/autocomplete": {
    parameters: {
      query?: never;
      header?: never;
      path?: never;
      cookie?: never;
    };
    /**
     * Autocomplete food names
     * @description **API key or client token.**
     *
     *     Lightweight food suggestions for a partial name, built for type-ahead ("ban" → banana, banana bread, …): generic foods first, then branded, each with its id, name, brand, a thumbnail and calories. Once the user picks one, fetch `GET /v1.2/foods/{food_id}` for servings and full nutrition. `items` is empty for fewer than 2 letters or digits, no match, or a search-index error (the suggestion service fails open so a typing user is not interrupted); an unreachable service still answers with the standard 502/504.
     *
     *     Callable with a client token carrying the `foods:read` scope.
     */
    get: operations["autocompleteFoods"];
    put?: never;
    post?: never;
    delete?: never;
    options?: never;
    head?: never;
    patch?: never;
    trace?: never;
  };
  "/v1.2/foods/{food_id}/alternatives": {
    parameters: {
      query?: never;
      header?: never;
      path?: never;
      cookie?: never;
    };
    get?: never;
    put?: never;
    /**
     * Suggest healthier alternatives for a food
     * @description **API key or client token.**
     *
     *     Returns healthier alternatives for a food, honoring the given dietary restrictions and preferences. Omit either array (or send `[]`) if it does not apply. An empty `alternatives` result is valid — no suitable alternatives were found.
     *
     *     Callable with a client token carrying the `foods:read` scope.
     */
    post: operations["suggestFoodAlternatives"];
    delete?: never;
    options?: never;
    head?: never;
    patch?: never;
    trace?: never;
  };
  "/v1.2/foods/barcode/{barcode}": {
    parameters: {
      query?: never;
      header?: never;
      path?: never;
      cookie?: never;
    };
    /**
     * Look up a food by barcode
     * @description **API key or client token.**
     *
     *     Exact lookup of the food a barcode names — one food, not a list. The `barcode` on the returned food is the database's normalized form and may differ from the digits you scanned in leading zeros, so display it rather than comparing it. For free-text search, use `GET /v1.2/foods` instead.
     *
     *     Callable with a client token carrying the `foods:read` scope.
     */
    get: operations["getFoodByBarcode"];
    put?: never;
    post?: never;
    delete?: never;
    options?: never;
    head?: never;
    patch?: never;
    trace?: never;
  };
  "/v1.2/foods/{food_id}": {
    parameters: {
      query?: never;
      header?: never;
      path?: never;
      cookie?: never;
    };
    /**
     * Get a food
     * @description **API key or client token.**
     *
     *     One food's full record — most importantly the **complete list of serving sizes**. Search, barcode, and food-analysis results carry a single default serving; fetch the food here to let an end user pick "1 cup" vs "100 g" vs "1 medium" when logging or predicting. Nutrition is per the default serving, in the shared nutrient vocabulary.
     *
     *     Callable with a client token carrying the `foods:read` scope.
     */
    get: operations["getFood"];
    put?: never;
    post?: never;
    delete?: never;
    options?: never;
    head?: never;
    patch?: never;
    trace?: never;
  };
  "/v1.2/restaurants": {
    parameters: {
      query?: never;
      header?: never;
      path?: never;
      cookie?: never;
    };
    /**
     * Search restaurants near a location
     * @description **API key or client token.**
     *
     *     Search restaurants matching `query` around (`latitude`, `longitude`), ranked by proximity. Every result is a restaurant — `type` is always `restaurant`. To search the dishes those restaurants serve, use `GET /v1.2/menu-items`.
     *
     *     Callable with a client token carrying the `restaurants:read` scope.
     */
    get: operations["searchRestaurants"];
    put?: never;
    post?: never;
    delete?: never;
    options?: never;
    head?: never;
    patch?: never;
    trace?: never;
  };
  "/v1.2/restaurants/{restaurant_id}/menu-items": {
    parameters: {
      query?: never;
      header?: never;
      path?: never;
      cookie?: never;
    };
    /**
     * List a restaurant's menu items
     * @description **API key or client token.**
     *
     *     The menu of one restaurant, by the `id` a `GET /v1.2/restaurants` result carries — a listing, not a search. Items come ordered by name with the nutrition their menu source publishes; each carries one serving, and `GET /v1.2/foods/{food_id}` returns the complete list. Page a long menu with `limit` and `offset`: a page shorter than `limit` is the last one. To find dishes across restaurants near a location, use `GET /v1.2/menu-items`.
     *
     *     Callable with a client token carrying the `restaurants:read` scope.
     */
    get: operations["listRestaurantMenuItems"];
    put?: never;
    post?: never;
    delete?: never;
    options?: never;
    head?: never;
    patch?: never;
    trace?: never;
  };
  "/v1.2/menu-items": {
    parameters: {
      query?: never;
      header?: never;
      path?: never;
      cookie?: never;
    };
    /**
     * Search menu items near a location
     * @description **API key or client token.**
     *
     *     Search dishes across restaurants near (`latitude`, `longitude`), with the nutrition each menu source publishes. Use `radius_meters` to widen or narrow the search, e.g. `radius_meters=5000` for 5 kilometers; each result reports its own `distance_meters`. To find the restaurants themselves, use `GET /v1.2/restaurants`.
     *
     *     Callable with a client token carrying the `restaurants:read` scope.
     */
    get: operations["searchMenuItems"];
    put?: never;
    post?: never;
    delete?: never;
    options?: never;
    head?: never;
    patch?: never;
    trace?: never;
  };
  "/v1.2/food-analysis/image": {
    parameters: {
      query?: never;
      header?: never;
      path?: never;
      cookie?: never;
    };
    get?: never;
    put?: never;
    /**
     * Analyze a food or label photo
     * @description **API key or client token.**
     *
     *     Analyzes a food photo and returns the detected foods with their nutrition and an aggregated total. The photo can show the food itself or a packaged product — the front of the pack, the ingredient list, or the Nutrition Facts panel all work, and a packaged product comes back as a single detection in the usual result shape. `image` accepts either an http(s) URL or a base64 data URI. Analysis can take tens of seconds for complex meals. Set `reasoning.effort` to `xhigh` to use the reasoning-based analyzer; omit `reasoning` or set its effort to `none` to use the standard analyzer. Both modes return the same response shape and use the same rate-limit bucket and credit cost.
     *
     *     **Beta:** label reading is in testing — returned nutrition can be incomplete or differ from the printed values, so validate results before relying on them. A photo of nothing but a barcode is rejected; use `GET /v1.2/foods/barcode/{barcode}` for those. Best results come from sharp, well-lit photos with the food or the complete panel large in the frame; ~1,024 px on the shorter side is plenty, and downsizing huge images lowers latency.
     *
     *     Callable with a client token carrying the `food_analysis:write` scope.
     */
    post: operations["analyzeFoodImage"];
    delete?: never;
    options?: never;
    head?: never;
    patch?: never;
    trace?: never;
  };
  "/v1.2/food-analysis/text": {
    parameters: {
      query?: never;
      header?: never;
      path?: never;
      cookie?: never;
    };
    get?: never;
    put?: never;
    /**
     * Analyze a meal description
     * @description **API key or client token.**
     *
     *     Parses free text like "a bowl of oatmeal with honey" into detected foods with quantities and nutrition — the text counterpart of `POST /v1.2/food-analysis/image`. Text analyses return `meal_name: null` (the caller already has the words) and grade nothing, so every detection carries `confidence: null`. For keyword search over the food database, use `GET /v1.2/foods`.
     *
     *     Callable with a client token carrying the `food_analysis:write` scope.
     */
    post: operations["analyzeFoodText"];
    delete?: never;
    options?: never;
    head?: never;
    patch?: never;
    trace?: never;
  };
  "/v1.2/food-analysis/corrections": {
    parameters: {
      query?: never;
      header?: never;
      path?: never;
      cookie?: never;
    };
    get?: never;
    put?: never;
    /**
     * Correct an analysis in plain English
     * @description **API key or client token.**
     *
     *     Revises an analysis result. Send back the `analysis` object exactly as `POST /v1.2/food-analysis/image` or `/text` returned it, plus `instruction` describing the correction; the response is a corrected result with recalculated totals. Adjust portions through `instruction` ("it was about half of that") rather than editing serving quantities by hand. Nutrient keys a detection omits are filled in as zero automatically, and each detection must carry at least one serving.
     *
     *     Callable with a client token carrying the `food_analysis:write` scope.
     */
    post: operations["correctFoodAnalysis"];
    delete?: never;
    options?: never;
    head?: never;
    patch?: never;
    trace?: never;
  };
  "/v1.2/food-logs": {
    parameters: {
      query?: never;
      header?: never;
      path?: never;
      cookie?: never;
    };
    /**
     * List a user's food logs in a date range
     * @description **API key or client token.**
     *
     *     Returns the logs between `start_date` and `end_date` (both inclusive local calendar dates in `timezone`; the range spans at most 60 days), ordered by timestamp. An empty list is a valid result.
     *
     *     Callable with a client token carrying the `food_logs:read` scope.
     */
    get: operations["listFoodLogs"];
    put?: never;
    /**
     * Log foods for a user
     * @description **API key or client token.**
     *
     *     Creates a food log from food + serving ids (from search or food-analysis results). The response echoes the log hydrated with full nutrition — save its `id` to update or delete the log, or fetch it again with GET /v1.2/food-logs/{log_id}. Not idempotent: verify with the list or get endpoint before retrying a timed-out create.
     *
     *     Callable with a client token carrying the `food_logs:write` scope.
     */
    post: operations["createFoodLog"];
    delete?: never;
    options?: never;
    head?: never;
    patch?: never;
    trace?: never;
  };
  "/v1.2/food-logs/{log_id}": {
    parameters: {
      query?: never;
      header?: never;
      path?: never;
      cookie?: never;
    };
    /**
     * Get a food log
     * @description **API key or client token.**
     *
     *     Fetches one food log by the id returned when it was created.
     *
     *     Callable with a client token carrying the `food_logs:read` scope.
     */
    get: operations["getFoodLog"];
    put?: never;
    post?: never;
    /**
     * Delete a food log
     * @description **API key or client token.**
     *
     *     Idempotent: deleting an unknown or already-deleted log answers the same 204, so it is safe to retry.
     *
     *     Callable with a client token carrying the `food_logs:write` scope.
     */
    delete: operations["deleteFoodLog"];
    options?: never;
    head?: never;
    /**
     * Update a food log
     * @description **API key or client token.**
     *
     *     Replaces any subset of the log: `foods`, `eaten_at`, `name`. Omitted fields are left unchanged.
     *
     *     Callable with a client token carrying the `food_logs:write` scope.
     */
    patch: operations["updateFoodLog"];
    trace?: never;
  };
  "/v1.2/glucose/predictions": {
    parameters: {
      query?: never;
      header?: never;
      path?: never;
      cookie?: never;
    };
    get?: never;
    put?: never;
    /**
     * Predict the glucose response to a meal
     * @description **API key or client token.**
     *
     *     Predicts the glucose curve a meal will produce for the given profile and body.timezone (required — the IANA timezone the end user is in; the prediction depends on the meal's local time of day). Optionally personalize by sending cgm_data with the consumed_foods eaten during it (both together; the upstream needs at least five complete days of paired history). Nothing is stored, so there is no end-user identity to send: the request acts as the partner itself.
     *
     *     Callable with a client token carrying the `glucose:read` scope.
     */
    post: operations["predictGlucoseResponse"];
    delete?: never;
    options?: never;
    head?: never;
    patch?: never;
    trace?: never;
  };
}
export type webhooks = Record<string, never>;
export interface components {
  schemas: {
    ChatErrorDetails: {
      message: string;
      type: string;
      param: string | null;
      code: string;
    };
    ChatError: {
      error: components["schemas"]["ChatErrorDetails"];
    };
    PublicChatMessage: {
      /** @enum {string} */
      role: "system" | "developer" | "user" | "assistant";
      /** @description Text only. All message content together is limited to 32768 UTF-8 bytes. */
      content: string;
    };
    ChatCompletionRequest: {
      /** @enum {string} */
      model: "january";
      /** @description Initial system/developer instructions, then user/assistant history. The final message must be user. */
      messages: components["schemas"]["PublicChatMessage"][];
      /** @description Omit this field. Streaming is not supported. */
      stream?: boolean;
    };
    ChatAssistantMessage: {
      /** @enum {string} */
      role: "assistant";
      content: string;
    };
    ChatCompletionChoice: {
      index: number;
      message: components["schemas"]["ChatAssistantMessage"];
      /** @enum {string} */
      finish_reason: "stop" | "length";
      logprobs: Record<string, never> | null;
    };
    ChatCompletionUsage: {
      prompt_tokens: number;
      completion_tokens: number;
      total_tokens: number;
    };
    ChatCompletionResponse: {
      id: string;
      /** @enum {string} */
      object: "chat.completion";
      created: number;
      /** @enum {string} */
      model: "january";
      choices: components["schemas"]["ChatCompletionChoice"][];
      /** @description Primary-model totals across every round; null if any round lacks usage. Excludes literature summarization. */
      usage: components["schemas"]["ChatCompletionUsage"] | null;
    };
    ApiError: {
      /**
       * @description A developer-facing explanation of what went wrong and how to fix it.
       * @example query is required: the food name to search for, e.g. ?query=greek yogurt.
       */
      message: string;
      /**
       * @description A stable machine-readable identifier for the class of failure — build retry logic on this, never on message wording.
       *
       *     Any request, each with the status it usually accompanies: `invalid_request` (400), `unauthorized` (401), `forbidden` (403), `not_found` (404), `payload_too_large` (413), `rate_limited` (429), `credit_limit_exceeded` (429), `internal_error` (500), `not_implemented` (501), `upstream_error` (502), `service_unavailable` (503), `upstream_timeout` (504). Those pairings are the common case, not a guarantee: a status we do not map falls back to `invalid_request` below 500 and `internal_error` at or above it, so an internal service answering 409 or 422 reaches you with that status and `code: invalid_request`. Branch on the code first and treat the status as the fallback, exactly as for a code you do not recognise.
       *
       *     Client tokens add six an API key never produces: `token_expired`, `token_invalid`, `token_revoked` (401), and `client_token_not_allowed`, `scope_insufficient`, `end_user_id_mismatch` (403). Each response documents its own.
       *
       *     Three more are specific to individual endpoints: `end_user_id_required` (400 — an sk- key called a food-log operation with no January-End-User-ID header), `date_range_too_large` (400 — a food-log date range past the documented maximum), and `client_token_revocation_incomplete` (503 — a revocation call that only stopped part of its batch; the same request is safe to repeat).
       *
       *     `POST /v1.2/food-analysis/image` adds four 400s about the image itself: `image_unreachable` (the URL could not be fetched), `image_corrupt` (the file could not be decoded), `image_format_unsupported` and `image_invalid_base64`. Each is fixed by the caller; the same image fails the same way again.
       *
       *     Retry only `rate_limited`, `internal_error`, `upstream_error`, `service_unavailable`, `upstream_timeout` and `client_token_revocation_incomplete`, with backoff — `not_implemented` is permanent until the feature ships, so its 5xx status is not a reason to retry it. Two more the status code alone gets wrong: `credit_limit_exceeded` is a 429 that **must never be retried** — the allowance returns next calendar month, so a client that backs off on every 429 will spin until then; and `token_expired` is refreshed, not retried — mint a new token, then retry once.
       *
       *     New codes may be added over time; treat an unknown code according to its HTTP status class.
       * @example invalid_request
       */
      code: string;
    };
    CreateClientTokenRequest: {
      /**
       * @description Your stable ID for the end user this token acts as. The token is bound to it; requests made with the token act only on this user.
       * @example acme-user-8271
       */
      end_user_id: string;
      /**
       * @description What the token may do. **Required** — name only the scopes this token needs (least privilege), never the full set out of convenience. A read-only food-lookup screen asks for `["foods:read"]`; a logging screen adds `food_logs:write`. Valid scopes: foods:read, food_analysis:write, food_logs:read, food_logs:write, glucose:read, restaurants:read.
       * @example [
       *       "foods:read"
       *     ]
       */
      scopes: (
        | "foods:read"
        | "food_analysis:write"
        | "food_logs:read"
        | "food_logs:write"
        | "glucose:read"
        | "restaurants:read"
      )[];
      /**
       * @description How long the token stays valid, in seconds. Between 300 and 7200; defaults to 1800.
       * @default 1800
       * @example 1800
       */
      ttl_seconds: number;
    };
    ClientToken: {
      /**
       * @description The credential itself. Shown exactly once — it is stored only as a hash and can never be retrieved again.
       * @example ct-4fQr7yNb2KcXm9TvLpZ3wHs6JdRg8AeYuQ1oViB5xCn
       */
      token: string;
      /**
       * @description Seconds until the token expires, counted from the moment this response was produced. Compute expiry from this rather than from `expires_at` — a device clock that is wrong makes an absolute timestamp wrong with it.
       * @example 1800
       */
      expires_in: number;
      /**
       * Format: date-time
       * @description The same expiry as an absolute UTC instant, for logs and humans.
       * @example 2026-08-26T18:35:11.000Z
       */
      expires_at: string;
      /**
       * @description The end user this token is bound to, echoed back so a caller can assert it minted what it meant to.
       * @example acme-user-8271
       */
      end_user_id: string;
      /**
       * @description What this token may do — the exact scopes it was granted, echoed back so a caller can assert it minted what it meant to.
       * @example [
       *       "foods:read",
       *       "food_logs:read"
       *     ]
       */
      scopes: (
        | "foods:read"
        | "food_analysis:write"
        | "food_logs:read"
        | "food_logs:write"
        | "glucose:read"
        | "restaurants:read"
      )[];
    };
    RevokeClientTokensRequest: {
      /**
       * @description The end user whose outstanding client tokens should all be revoked.
       * @example acme-user-8271
       */
      end_user_id: string;
    };
    ClientTokenRevocationResult: {
      /**
       * @description How many live tokens this call revoked. Already-revoked and already-expired tokens are not counted, so an immediate second call reports 0 — repeat the call until it does.
       * @example 2
       */
      revoked_count: number;
    };
    CreditBalance: {
      /**
       * @description The plan this allowance comes from.
       * @example free
       */
      plan: string;
      /**
       * Format: date
       * @description First day of the current billing period (UTC), inclusive.
       * @example 2026-08-01
       */
      period_start: string;
      /**
       * Format: date
       * @description Last day of the current billing period (UTC), inclusive.
       * @example 2026-08-31
       */
      period_end: string;
      /**
       * Format: date-time
       * @description When the allowance resets and `used_credits` returns to 0.
       * @example 2026-09-01T00:00:00.000Z
       */
      resets_at: string;
      /**
       * @description Credits included in the plan for this period, or `null` when the plan has no ceiling.
       * @example 1000
       */
      included_credits: number | null;
      /**
       * @description Credits used so far this period. Billable operations consume credits — how many depends on the operation and your plan — while failed calls cost nothing.
       * @example 342
       */
      used_credits: number;
      /**
       * @description Credits left in this period, or `null` when the plan has no ceiling.
       * @example 658
       */
      remaining_credits: number | null;
    };
    NutrientAmount: {
      /** @example 300 */
      value: number;
      /**
       * @description Canonical across the API: g, mg, kcal, IU.
       * @example g
       */
      unit: string;
    };
    Nutrients: {
      calories?: components["schemas"]["NutrientAmount"];
      protein?: components["schemas"]["NutrientAmount"];
      carbohydrates?: components["schemas"]["NutrientAmount"];
      net_carbohydrates?: components["schemas"]["NutrientAmount"];
      total_fat?: components["schemas"]["NutrientAmount"];
      trans_fat?: components["schemas"]["NutrientAmount"];
      saturated_fat?: components["schemas"]["NutrientAmount"];
      fiber?: components["schemas"]["NutrientAmount"];
      total_sugars?: components["schemas"]["NutrientAmount"];
      added_sugars?: components["schemas"]["NutrientAmount"];
      cholesterol?: components["schemas"]["NutrientAmount"];
      calcium?: components["schemas"]["NutrientAmount"];
      iron?: components["schemas"]["NutrientAmount"];
      potassium?: components["schemas"]["NutrientAmount"];
      sodium?: components["schemas"]["NutrientAmount"];
      vitamin_d?: components["schemas"]["NutrientAmount"];
    };
    FoodServing: {
      /**
       * @description Opaque serving id; may look numeric but is always a string.
       * @example 68051535
       */
      id: string | null;
      /** @example 1 */
      quantity: number | null;
      /** @example oz */
      unit: string | null;
      /**
       * @description Multiplier applied to the food's nutrition values for this serving.
       * @example 1
       */
      scaling_factor: number | null;
      /** @example 28 */
      weight_grams: number | null;
      /**
       * @description Whether this is the default serving for the food.
       * @example true
       */
      is_primary: boolean | null;
    };
    Food: {
      /**
       * @description Opaque id — pass it back verbatim to log, predict, or fetch this food.
       * @example 101963552
       */
      id: string;
      /**
       * @description What kind of food this is: `generic` for a database staple ("banana"), `branded` for a packaged product, `recipe` for a multi-ingredient dish.
       * @example branded
       * @enum {string}
       */
      type: "generic" | "branded" | "recipe";
      /**
       * @description Null only when the database has no name for the row.
       * @example Dipped Banana Bites
       */
      name: string | null;
      /**
       * @description null for generic foods and recipes, which have no brand.
       * @example Banana
       */
      brand_name: string | null;
      /** @description Per-serving nutrition in the shared nutrient vocabulary. Keys are omitted when the database has no value. */
      nutrients: components["schemas"]["Nutrients"];
      /** @description Glycemic index; null when the database has none for this food. */
      glycemic_index: number | null;
      /** @description Glycemic load; null when the database has none for this food. */
      glycemic_load: number | null;
      /** @description URL of a picture of the food; null when the database has none. */
      image_url: string | null;
      /**
       * @description The product's barcode; null for foods that have none. Named for the code rather than one of its encodings — the same field carries a UPC, an EAN or a GTIN. It is the database's normalized form, so it may differ from the digits you scanned in leading zeros: display it, do not string-compare it.
       * @example 049000006346
       */
      barcode: string | null;
      /** @description Search and barcode results carry the default serving only; `GET /v1.2/foods/{food_id}` returns the complete list to choose from. */
      servings: components["schemas"]["FoodServing"][];
    };
    FoodSearchResponse: {
      /** @description Ranked matches, best first. Empty when nothing matches — not an error. */
      items: components["schemas"]["Food"][];
    };
    FoodSuggestion: {
      /**
       * @description Opaque id — the same id `GET /v1.2/foods/{food_id}` takes.
       * @example 70376053
       */
      id: string;
      /**
       * @description What kind of food this is: `generic` for a database staple ("banana"), `branded` for a packaged product, `recipe` for a multi-ingredient dish.
       * @example generic
       * @enum {string}
       */
      type: "generic" | "branded";
      /**
       * @description Generic foods are lowercase; branded foods keep their product name. Null only when the index has no name for the row.
       * @example greek yogurt
       */
      name: string | null;
      /**
       * @description null for generic (non-branded) foods.
       * @example Chobani
       */
      brand_name: string | null;
      /** @description Thumbnail of the food; null when the database has none. */
      image_url: string | null;
      /** @description Calories per default serving, in the shared nutrient vocabulary — the one nutrient a suggestion carries. Fetch the food for the full panel. Empty when the index has no value. */
      nutrients: components["schemas"]["Nutrients"];
    };
    FoodSuggestionsResponse: {
      /** @description Ranked suggestions, generic foods before branded. Empty when nothing matches. */
      items: components["schemas"]["FoodSuggestion"][];
    };
    SuggestFoodAlternativesRequest: {
      /**
       * @description Allergens/ingredients to avoid. Omit it (or send []) if none apply.
       * @example [
       *       "gluten",
       *       "peanuts"
       *     ]
       */
      diet_restrictions?: (
        | "gluten"
        | "lactose"
        | "yeast"
        | "tree_nuts"
        | "peanuts"
        | "dairy"
        | "eggs"
        | "sulfites"
        | "soy"
        | "wheat"
        | "shellfish"
        | "fish"
        | "mushrooms"
        | "sesame"
        | "msg"
        | "caffeine"
        | "fodmaps"
      )[];
      /**
       * @description Dietary patterns to match. Omit it (or send []) if none apply.
       * @example [
       *       "vegetarian"
       *     ]
       */
      diet_preferences?: (
        | "vegetarian"
        | "vegan"
        | "keto"
        | "paleo"
        | "pescatarian"
        | "low_carbohydrate"
        | "high_protein"
        | "kosher"
        | "halal"
      )[];
    };
    ServingSummary: {
      /**
       * @description Null only when the producer sent a serving with no id.
       * @example 34237662
       */
      id: string | null;
      /**
       * @description How much of `unit` this serving is; null when the producer reported none.
       * @example 1
       */
      quantity: number | null;
      /**
       * @description Null only when the producer sent a serving with no unit.
       * @example cup
       */
      unit: string | null;
    };
    AlternativeFood: {
      /**
       * @description Catalog food id, or null when the producer matched none.
       * @example 70379835
       */
      id: string | null;
      /**
       * @description Null only when the producer sent a food with no name.
       * @example Oatmeal
       */
      name: string | null;
      /**
       * @description Null for generic (non-branded) foods.
       * @example null
       */
      brand_name: string | null;
      nutrients: components["schemas"]["Nutrients"];
      /** @description Servings to read the nutrition against. Empty when the recommender returned none — the key itself is always present. */
      servings: components["schemas"]["ServingSummary"][];
    };
    FoodAlternatives: {
      /** @description Healthier alternatives matching the restrictions and preferences. An empty array is a valid result, not an error. */
      alternatives: components["schemas"]["AlternativeFood"][];
    };
    Restaurant: {
      /**
       * @description Always `restaurant`: this endpoint answers places only. For dishes, use `GET /v1.2/menu-items`.
       * @example restaurant
       * @enum {string}
       */
      type: "restaurant";
      /** @example 53fc3b8a-e6bf-404d-83c8-9f42124d1bee */
      id: string;
      /**
       * @description Null only when the source has no name for the place.
       * @example McDonald's
       */
      name: string | null;
      /**
       * @description Whether this location belongs to a chain; null when unknown.
       * @example false
       */
      is_chain: boolean | null;
      /**
       * @description Distance from (latitude, longitude) in meters; null when the source cannot place this result.
       * @example 124
       */
      distance_meters: number | null;
      /** @example San Francisco */
      city: string | null;
      /** @example 123 Main Street */
      address1: string | null;
      /**
       * @description Second address line; null when there is none.
       * @example Suite 100
       */
      address2: string | null;
    };
    RestaurantSearchResponse: {
      /** @description Matches ranked by proximity. Empty when nothing matches — not an error. */
      items: components["schemas"]["Restaurant"][];
    };
    RestaurantMenuItem: {
      /**
       * @description Food id of the dish — the same id `GET /v1.2/foods/{food_id}` and `POST /v1.2/food-logs` take. Null only when the menu source carries no id for the row.
       * @example 228990954
       */
      id: string | null;
      /**
       * @description Null only when the menu source has no name for the dish.
       * @example Cheeseburger
       */
      name: string | null;
      /** @description Per-serving nutrition in the shared nutrient vocabulary. Keys are omitted when the menu source has no value. */
      nutrients: components["schemas"]["Nutrients"];
      /** @description Glycemic index; null when the source has none. */
      glycemic_index: number | null;
      /** @description Glycemic load; null when the source has none. */
      glycemic_load: number | null;
      /** @description The serving the nutrition is given for. `GET /v1.2/foods/{food_id}` returns the complete list of servings. */
      servings: components["schemas"]["FoodServing"][];
    };
    RestaurantMenuItemsResponse: {
      /** @description The menu items, by name. Empty when the restaurant has no menu on record. */
      items: components["schemas"]["RestaurantMenuItem"][];
    };
    MenuItem: {
      /**
       * @description Always `menu_item`: this endpoint answers dishes only. For the places themselves, use `GET /v1.2/restaurants`.
       * @example menu_item
       * @enum {string}
       */
      type: "menu_item";
      /** @example 228990954 */
      id: string;
      /**
       * @description Null only when the menu source has no name for the dish.
       * @example burger
       */
      name: string | null;
      /**
       * @description Null when the menu source names no restaurant.
       * @example morning due cafe
       */
      restaurant_name: string | null;
      /**
       * @description Whether the restaurant belongs to a chain; null when unknown.
       * @example false
       */
      is_chain: boolean | null;
      /** @description Per-dish nutrition in the shared nutrient vocabulary. Keys are omitted when the menu source has no value. */
      nutrients: components["schemas"]["Nutrients"];
      /** @description Glycemic index; null when the source has none. */
      glycemic_index: number | null;
      /** @description Glycemic load; null when the source has none. */
      glycemic_load: number | null;
      /** @description URL of a picture of the dish; null when the source has none. */
      image_url: string | null;
      /**
       * @description Distance from (latitude, longitude) in meters.
       * @example 124
       */
      distance_meters: number | null;
      servings: components["schemas"]["FoodServing"][];
    };
    MenuItemSearchResponse: {
      /** @description Matching dishes ranked by proximity. Empty when nothing matches — not an error. */
      items: components["schemas"]["MenuItem"][];
    };
    Reasoning: {
      /**
       * @description `none` uses the standard analyzer; `xhigh` uses the reasoning-based analyzer.
       * @example xhigh
       * @enum {string}
       */
      effort: "none" | "xhigh";
    };
    ImageFoodAnalysisRequest: {
      /**
       * @description The food photo — the food itself or a packaged product's label — as an http(s) URL or a base64 data URI (data:image/jpeg;base64,…). Formats: JPG, PNG, WEBP, and non-animated GIF. Around 1,024 px on the shorter side is enough for reliable results (a recommendation, not a validation rule). A URL must be publicly fetchable server-side — hosts that block hotlinking or require a login cannot be read — and has no enforced size cap, though very large files slow the analysis and can time out. Base64 must be a complete data URI and fit the 5 MB request-body cap, so keep raw images under ~3.5 MB before encoding (base64 inflates by ~33%). Prefer the URL when the image is already hosted.
       * @example https://i.imgur.com/bTQIGxf.png
       */
      image: string;
      /**
       * @description Controls analysis effort. Omit it or set `effort` to `none` to use the standard analyzer; `xhigh` uses the reasoning-based analyzer. Both modes return the same FoodAnalysisResult shape and use the same rate-limit bucket and credit cost.
       * @default {
       *       "effort": "none"
       *     }
       */
      reasoning: components["schemas"]["Reasoning"];
    };
    DetectionServing: {
      /**
       * @description Null only when the producer sent a serving with no id.
       * @example 34237662
       */
      id: string | null;
      /**
       * @description How much of `unit` this serving is; null when the producer reported none.
       * @example 1
       */
      quantity: number | null;
      /**
       * @description Null only when the producer sent a serving with no unit.
       * @example cup
       */
      unit: string | null;
      /**
       * @description Quantity parsed from the text ('2 cups' → 2); null on image analyses. Advisory — corrections reads the serving's own quantity.
       * @example 2
       */
      selected_quantity: number | null;
    };
    DetectedFood: {
      /**
       * @description Catalog food id, or null when the producer matched none.
       * @example 70379835
       */
      id: string | null;
      /**
       * @description Null only when the producer sent a food with no name.
       * @example Oatmeal
       */
      name: string | null;
      /**
       * @description Null for generic (non-branded) foods.
       * @example null
       */
      brand_name: string | null;
      nutrients: components["schemas"]["Nutrients"];
      /** @description Never empty: every detection producer guarantees at least one serving. */
      servings: components["schemas"]["DetectionServing"][];
    };
    Detection: {
      /**
       * @description How sure the vision model is of this detection. Null on text analyses and on corrected results.
       * @example high
       * @enum {string|null}
       */
      confidence: "high" | "medium" | "low" | null;
      food: components["schemas"]["DetectedFood"];
    };
    FoodAnalysisResult: {
      /**
       * @description A name for the meal as a whole. Null on text analyses — the caller already has the words.
       * @example Breakfast Bowl
       */
      meal_name: string | null;
      /** @description Aggregated nutrition across all detections. Always present; individual keys are omitted when no producer had a value. */
      total_nutrients: components["schemas"]["Nutrients"];
      /** @description Detected foods. Always present — an empty array means nothing was recognized. */
      detections: components["schemas"]["Detection"][];
    };
    TextFoodAnalysisRequest: {
      /**
       * @description Natural-language description of what was eaten; parsed into detected foods with quantities.
       * @example a bowl of oatmeal with honey and a banana
       */
      text: string;
    };
    CorrectFoodAnalysisRequest: {
      /** @description The result from `POST /v1.2/food-analysis/image` or `/text`, sent back exactly as it was returned. Omitted zero-value nutrient keys are filled in automatically, and `total_nutrients` is recalculated rather than trusted — send it or leave it out, it makes no difference. */
      analysis: components["schemas"]["FoodAnalysisResult"];
      /**
       * @description Plain-English description of what to correct.
       * @example The oatmeal was steel-cut, about 2 cups, and there was no honey.
       */
      instruction: string;
    };
    FoodSelection: {
      /**
       * @description Food id from a search or food-analysis result.
       * @example 101963552
       */
      food_id: string;
      /**
       * @description One of the food's serving ids.
       * @example 68051535
       */
      serving_id: string;
      /**
       * @description How many of that serving were consumed.
       * @example 1.4
       */
      quantity: number;
    };
    CreateFoodLogRequest: {
      foods: components["schemas"]["FoodSelection"][];
      /**
       * Format: date-time
       * @description When the meal was eaten — any ISO-8601 offset; stored and returned in UTC with milliseconds. Omitted = now.
       * @example 2024-09-13T11:34:56Z
       */
      eaten_at?: string;
      /** @example Breakfast */
      name?: string;
    };
    ServingDetails: {
      /**
       * @description Null only when the upstream sent a serving with no id.
       * @example 68051535
       */
      id: string | null;
      /**
       * @description How many units make up one of this serving, e.g. 1 for "1 cup". Null when the upstream reported none.
       * @example 1
       */
      quantity: number | null;
      /**
       * @description Null only when the upstream sent a serving with no unit.
       * @example cup
       */
      unit: string | null;
      /**
       * @description Null when the upstream has no gram weight for this serving.
       * @example 245
       */
      weight_grams: number | null;
    };
    LoggedFood: {
      /**
       * @description Food id from a search or food-analysis result. Null only when the upstream sent a food with no id.
       * @example 101963552
       */
      food_id: string | null;
      /**
       * @description Null only when the upstream sent none.
       * @example Greek Yogurt, Plain, Whole Milk
       */
      name: string | null;
      /**
       * @description Null for generic (non-branded) foods.
       * @example null
       */
      brand_name: string | null;
      /** @example null */
      image_url: string | null;
      /** @example 11.3 */
      glycemic_index: number | null;
      /** @example 1.4 */
      glycemic_load: number | null;
      /** @description Scaled to the consumed quantity. */
      nutrients: components["schemas"]["Nutrients"];
      /**
       * @description How many of the serving below were consumed. Null only when the upstream sent no consumed quantity.
       * @example 1.4
       */
      quantity: number | null;
      /** @description The serving definition the quantity refers to. */
      serving: components["schemas"]["ServingDetails"];
    };
    FoodLog: {
      /**
       * @description Save this id to update or delete the log. Null only when the upstream sent a log with no id — such a log cannot be addressed.
       * @example 78129823-8ba2-4183-b13b-71f0e963c606
       */
      id: string | null;
      foods: components["schemas"]["LoggedFood"][];
      /**
       * Format: date-time
       * @description When the meal was eaten. UTC, with milliseconds.
       * @example 2024-09-13T11:34:56.000Z
       */
      eaten_at: string;
      /**
       * @description Null when no name was given.
       * @example Breakfast
       */
      name: string | null;
    };
    FoodLogList: {
      /** @description Logs in the range, ordered by timestamp. An empty list is a valid result. */
      items: components["schemas"]["FoodLog"][];
    };
    UpdateFoodLogRequest: {
      foods?: components["schemas"]["FoodSelection"][];
      /**
       * Format: date-time
       * @description When the meal was eaten — any ISO-8601 offset; stored and returned in UTC with milliseconds. Omit to leave it unchanged.
       * @example 2024-09-13T11:34:56Z
       */
      eaten_at?: string;
      /** @example Breakfast */
      name?: string;
    };
    Height: {
      /** @example 66 */
      value: number;
      /**
       * @example in
       * @enum {string}
       */
      unit: "in" | "cm";
    };
    Weight: {
      /** @example 150 */
      value: number;
      /**
       * @example lb
       * @enum {string}
       */
      unit: "lb" | "kg";
    };
    GlucoseUserProfile: {
      /** @example 42 */
      age: number;
      /**
       * @description Biological sex, as consumed by the prediction model.
       * @example female
       * @enum {string}
       */
      sex: "male" | "female";
      height: components["schemas"]["Height"];
      weight: components["schemas"]["Weight"];
      /**
       * @example moderately_active
       * @enum {string}
       */
      activity_level?:
        "sedentary" | "lightly_active" | "moderately_active" | "very_active";
      /**
       * @description Omit it (or send []) if none apply. Type 1 diabetes is not supported by the prediction model.
       * @example [
       *       "prediabetes"
       *     ]
       */
      health_conditions?: ("type_2_diabetes" | "prediabetes")[];
    };
    CgmReading: {
      /**
       * Format: date-time
       * @description When the reading was taken. Must carry a timezone designator.
       * @example 2024-09-10T08:15:00Z
       */
      timestamp: string;
      /**
       * @description mg/dL. At most one reading per 15-minute window.
       * @example 104
       */
      value: number;
    };
    ConsumedFood: {
      /**
       * Format: date-time
       * @description When the food was eaten. Must carry a timezone designator.
       * @example 2024-09-10T08:00:00Z
       */
      timestamp: string;
      /**
       * @description Food id from a search or food-analysis result.
       * @example 101963552
       */
      food_id: string;
      /**
       * @description One of the food's serving ids.
       * @example 68051535
       */
      serving_id: string;
      /**
       * @description How many of that serving were consumed.
       * @example 1.4
       */
      quantity: number;
    };
    GlucosePredictionRequest: {
      user_profile: components["schemas"]["GlucoseUserProfile"];
      /**
       * @description The IANA timezone the end user is in. The prediction depends on the meal's local time of day.
       * @example America/New_York
       */
      timezone: string;
      /** @description The meal to predict the glucose response for. */
      foods: components["schemas"]["FoodSelection"][];
      /**
       * Format: date-time
       * @description When the meal is (or will be) eaten. Must carry a timezone designator.
       * @example 2024-09-13T11:34:56Z
       */
      start_time: string;
      /** @description Optional CGM history, to personalize the prediction to this end user. Send it together with `consumed_foods` covering the same period — **at least 5 complete days of paired history**, which is what the model needs to train on them. Fewer is refused with `invalid_request`. Omit both fields for a standard prediction, which needs no sensor and no history. */
      cgm_data?: components["schemas"]["CgmReading"][];
      /** @description The meals eaten during the CGM history; requires cgm_data. */
      consumed_foods?: components["schemas"]["ConsumedFood"][];
    };
    GlucosePredictionPoint: {
      /**
       * @description Minutes after start_time.
       * @example 30
       */
      minutes: number;
      /**
       * @description Predicted glucose, mg/dL.
       * @example 140
       */
      value: number;
    };
    GlucoseChart: {
      /**
       * @description Suggested Y-axis lower bound (mg/dL). A fixed target-range bound, not the minimum of the curve. Null when the model sent none.
       * @example 70
       */
      min: number | null;
      /**
       * @description Suggested Y-axis upper bound (mg/dL): 180 with Type 2 diabetes in health_conditions, otherwise 140. Not the maximum of the curve. Null when the model sent none.
       * @example 140
       */
      max: number | null;
    };
    GlucosePrediction: {
      /** @description The points of the predicted glucose curve, at 15-minute intervals starting at start_time. */
      points: components["schemas"]["GlucosePredictionPoint"][];
      /**
       * @description The meal's overall glucose impact. Null when the model returned no grade, or one outside this list.
       * @example low
       * @enum {string|null}
       */
      impact_score: "low" | "medium" | "high" | null;
      chart: components["schemas"]["GlucoseChart"];
    };
  };
  responses: never;
  parameters: {
    /**
     * @description Your stable ID for the end user whose food logs this request reads or writes. Opaque to January — use the same ID your system already uses for them.
     *
     *     | Credential | Header | Result |
     *     | --- | --- | --- |
     *     | API key (`sk-…`) | absent | `400 end_user_id_required` |
     *     | API key (`sk-…`) | present | the request acts on that end user |
     *     | Client token (`ct-…`) | absent | filled in from the token |
     *     | Client token (`ct-…`) | the end user the token is bound to | accepted |
     *     | Client token (`ct-…`) | any other end user | `403 end_user_id_mismatch` |
     */
    JanuaryEndUserId: string;
  };
  requestBodies: never;
  headers: never;
  pathItems: never;
}
export type $defs = Record<string, never>;
export interface operations {
  createChatCompletion: {
    parameters: {
      query?: never;
      header?: never;
      path?: never;
      cookie?: never;
    };
    requestBody: {
      content: {
        "application/json": components["schemas"]["ChatCompletionRequest"];
      };
    };
    responses: {
      200: {
        headers: {
          [name: string]: unknown;
        };
        content: {
          "application/json": components["schemas"]["ChatCompletionResponse"];
        };
      };
      400: {
        headers: {
          [name: string]: unknown;
        };
        content: {
          "application/json": components["schemas"]["ChatError"];
        };
      };
      /**
       * @description The request carried no `Authorization` header, or the credential in it was rejected.
       *
       *     `unauthorized` — the header is missing or malformed, or the key is not one we recognise. A valid key belonging to the other API version is `403 forbidden` instead.
       *
       *     A client token is rejected in one of three ways, and only the first should be handled automatically:
       *
       *     - `token_expired` — the token is past its TTL. Mint a fresh one from your backend and retry the request once. This is routine and expected once per TTL window.
       *     - `token_invalid` — no such token: it was never issued, or it has been purged, which happens shortly after it expires. This code is not an automatic token-refresh signal.
       *     - `token_revoked` — the token was revoked, by `POST /v1.2/auth/client-token-revocations` or from the dashboard. The end user signs in again in your app, and your backend decides whether to mint another; a device that just re-mints defeats the revocation.
       */
      401: {
        headers: {
          /** @description RFC 6750 challenge. `Bearer` when the request carried no credential; `Bearer error="invalid_token", error_description="token expired|token invalid|token revoked"` when one was rejected. */
          "WWW-Authenticate"?: string;
          [name: string]: unknown;
        };
        content: {
          "application/json": components["schemas"]["ChatError"];
        };
      };
      /**
       * @description The credential is valid but is not allowed to make this request. `forbidden` is the general case — a key issued for the other API version, for example. A client token adds `client_token_not_allowed`, meaning the endpoint takes only an `sk-` API key — its description opens with **API key only.** (in this API: `POST /v1.2/auth/client-tokens`, `POST /v1.2/auth/client-token-revocations`, `POST /v1.2/chat/completions`, and `GET /v1.2/credits`).
       *
       *     On an endpoint that opens with **API key or client token.**, `scope_insufficient` means the token was minted without the scope named at the end of that endpoint’s description.
       */
      403: {
        headers: {
          [name: string]: unknown;
        };
        content: {
          "application/json": components["schemas"]["ChatError"];
        };
      };
      /** @description Either a rate limit was exceeded (`code: rate_limited`) or the monthly credit allowance is spent (`code: credit_limit_exceeded`). When Retry-After is present, wait that many seconds; a per-day allowance resets 24 hours after the first request in its window. Credit exhaustion carries no Retry-After and retrying does not help — the allowance returns at the start of the next calendar month. Call `GET /v1.2/credits` for the balance and reset date. */
      429: {
        headers: {
          /** @description Seconds to wait before retrying; present when the window is known. */
          "Retry-After"?: string;
          [name: string]: unknown;
        };
        content: {
          "application/json": components["schemas"]["ChatError"];
        };
      };
      /** @description OpenAI-shaped error: { error: { message, type, param, code } }. */
      default: {
        headers: {
          [name: string]: unknown;
        };
        content: {
          "application/json": components["schemas"]["ChatError"];
        };
      };
    };
  };
  createClientToken: {
    parameters: {
      query?: never;
      header?: never;
      path?: never;
      cookie?: never;
    };
    requestBody: {
      content: {
        "application/json": components["schemas"]["CreateClientTokenRequest"];
      };
    };
    responses: {
      /** @description Token minted. The raw `token` is returned exactly once — relay it to the device and store nothing; only its hash is kept. The response also echoes `expires_in`/`expires_at`, the bound `end_user_id`, and the exact `scopes` granted. */
      201: {
        headers: {
          [name: string]: unknown;
        };
        content: {
          "application/json": components["schemas"]["ClientToken"];
        };
      };
      /**
       * @description The request carried no `Authorization` header, or the credential in it was rejected.
       *
       *     `unauthorized` — the header is missing or malformed, or the key is not one we recognise. A valid key belonging to the other API version is `403 forbidden` instead.
       *
       *     A client token is rejected in one of three ways, and only the first should be handled automatically:
       *
       *     - `token_expired` — the token is past its TTL. Mint a fresh one from your backend and retry the request once. This is routine and expected once per TTL window.
       *     - `token_invalid` — no such token: it was never issued, or it has been purged, which happens shortly after it expires. This code is not an automatic token-refresh signal.
       *     - `token_revoked` — the token was revoked, by `POST /v1.2/auth/client-token-revocations` or from the dashboard. The end user signs in again in your app, and your backend decides whether to mint another; a device that just re-mints defeats the revocation.
       */
      401: {
        headers: {
          /** @description RFC 6750 challenge. `Bearer` when the request carried no credential; `Bearer error="invalid_token", error_description="token expired|token invalid|token revoked"` when one was rejected. */
          "WWW-Authenticate"?: string;
          [name: string]: unknown;
        };
        content: {
          "application/json": components["schemas"]["ApiError"];
        };
      };
      /**
       * @description `forbidden` — client tokens have been switched off for your account (turn them back on in the [Developer Dashboard](https://dashboard.january.ai)), or the key is issued for the other API version.
       *
       *     `client_token_not_allowed` — this request was made with a client token, and minting requires your `sk-` API key.
       */
      403: {
        headers: {
          [name: string]: unknown;
        };
        content: {
          "application/json": components["schemas"]["ApiError"];
        };
      };
      /** @description Minting is capped per partner (`code: rate_limited`) — the only limit that applies here. It is `@NotBillable` and outside the shared v1.2 request ceiling, so this 429 never means credit exhaustion or the daily ceiling. Honor `Retry-After`; mint one token per user session and reuse it until it expires rather than minting per request. */
      429: {
        headers: {
          /** @description Seconds to wait before retrying. */
          "Retry-After"?: string;
          [name: string]: unknown;
        };
        content: {
          "application/json": components["schemas"]["ApiError"];
        };
      };
      /** @description Client tokens are not configured on this environment. */
      503: {
        headers: {
          [name: string]: unknown;
        };
        content: {
          "application/json": components["schemas"]["ApiError"];
        };
      };
      /** @description Any other error: the HTTP status plus { code, message }. Retry only rate_limited and the transient 5xx codes. */
      default: {
        headers: {
          [name: string]: unknown;
        };
        content: {
          "application/json": components["schemas"]["ApiError"];
        };
      };
    };
  };
  revokeClientTokens: {
    parameters: {
      query?: never;
      header?: never;
      path?: never;
      cookie?: never;
    };
    requestBody: {
      content: {
        "application/json": components["schemas"]["RevokeClientTokensRequest"];
      };
    };
    responses: {
      /** @description Revocation processed. `revoked_count` is how many live tokens this call actually stopped — repeat the call until it reports 0, since already-revoked and already-expired tokens are not counted. */
      200: {
        headers: {
          [name: string]: unknown;
        };
        content: {
          "application/json": components["schemas"]["ClientTokenRevocationResult"];
        };
      };
      /**
       * @description The request carried no `Authorization` header, or the credential in it was rejected.
       *
       *     `unauthorized` — the header is missing or malformed, or the key is not one we recognise. A valid key belonging to the other API version is `403 forbidden` instead.
       *
       *     A client token is rejected in one of three ways, and only the first should be handled automatically:
       *
       *     - `token_expired` — the token is past its TTL. Mint a fresh one from your backend and retry the request once. This is routine and expected once per TTL window.
       *     - `token_invalid` — no such token: it was never issued, or it has been purged, which happens shortly after it expires. This code is not an automatic token-refresh signal.
       *     - `token_revoked` — the token was revoked, by `POST /v1.2/auth/client-token-revocations` or from the dashboard. The end user signs in again in your app, and your backend decides whether to mint another; a device that just re-mints defeats the revocation.
       */
      401: {
        headers: {
          /** @description RFC 6750 challenge. `Bearer` when the request carried no credential; `Bearer error="invalid_token", error_description="token expired|token invalid|token revoked"` when one was rejected. */
          "WWW-Authenticate"?: string;
          [name: string]: unknown;
        };
        content: {
          "application/json": components["schemas"]["ApiError"];
        };
      };
      /**
       * @description `forbidden` — the key is issued for the other API version. Unlike minting, revocation is **not** gated on your account’s client-token switch ([Developer Dashboard](https://dashboard.january.ai)): switching minting off must never take away the ability to revoke what is already out there.
       *
       *     `client_token_not_allowed` — this request was made with a client token, and revocation requires your `sk-` API key.
       */
      403: {
        headers: {
          [name: string]: unknown;
        };
        content: {
          "application/json": components["schemas"]["ApiError"];
        };
      };
      /** @description Revocation is capped per partner (`code: rate_limited`) — the only limit that applies here. It is `@NotBillable` and outside the shared v1.2 request ceiling, so this 429 never means credit exhaustion or the daily ceiling, and a partner mid-incident can still cut a device off. Honor `Retry-After` and retry; the revoke bucket is separate from minting, so a security sweep cannot spend your sign-in allowance. */
      429: {
        headers: {
          /** @description Seconds to wait before retrying. */
          "Retry-After"?: string;
          [name: string]: unknown;
        };
        content: {
          "application/json": components["schemas"]["ApiError"];
        };
      };
      /** @description `service_unavailable` — client tokens are not configured on this environment. `client_token_revocation_incomplete` — some tokens could not be revoked; the message names how many succeeded, and repeating the request is safe and picks up the remainder. */
      503: {
        headers: {
          [name: string]: unknown;
        };
        content: {
          "application/json": components["schemas"]["ApiError"];
        };
      };
      /** @description Any other error: the HTTP status plus { code, message }. Retry only rate_limited and the transient 5xx codes. */
      default: {
        headers: {
          [name: string]: unknown;
        };
        content: {
          "application/json": components["schemas"]["ApiError"];
        };
      };
    };
  };
  getCredits: {
    parameters: {
      query?: never;
      header?: never;
      path?: never;
      cookie?: never;
    };
    requestBody?: never;
    responses: {
      /** @description Your credit balance for the current billing period: the `plan`, the period bounds and reset instant, `used_credits`, and `included_credits`/`remaining_credits` — always present, and `null` for an uncapped partner whose plan has no ceiling. */
      200: {
        headers: {
          [name: string]: unknown;
        };
        content: {
          "application/json": components["schemas"]["CreditBalance"];
        };
      };
      /**
       * @description The request carried no `Authorization` header, or the credential in it was rejected.
       *
       *     `unauthorized` — the header is missing or malformed, or the key is not one we recognise. A valid key belonging to the other API version is `403 forbidden` instead.
       *
       *     A client token is rejected in one of three ways, and only the first should be handled automatically:
       *
       *     - `token_expired` — the token is past its TTL. Mint a fresh one from your backend and retry the request once. This is routine and expected once per TTL window.
       *     - `token_invalid` — no such token: it was never issued, or it has been purged, which happens shortly after it expires. This code is not an automatic token-refresh signal.
       *     - `token_revoked` — the token was revoked, by `POST /v1.2/auth/client-token-revocations` or from the dashboard. The end user signs in again in your app, and your backend decides whether to mint another; a device that just re-mints defeats the revocation.
       */
      401: {
        headers: {
          /** @description RFC 6750 challenge. `Bearer` when the request carried no credential; `Bearer error="invalid_token", error_description="token expired|token invalid|token revoked"` when one was rejected. */
          "WWW-Authenticate"?: string;
          [name: string]: unknown;
        };
        content: {
          "application/json": components["schemas"]["ApiError"];
        };
      };
      /**
       * @description The credential is valid but is not allowed to make this request. `forbidden` is the general case — a key issued for the other API version, for example. A client token adds `client_token_not_allowed`, meaning the endpoint takes only an `sk-` API key — its description opens with **API key only.** (in this API: `POST /v1.2/auth/client-tokens`, `POST /v1.2/auth/client-token-revocations`, `POST /v1.2/chat/completions`, and `GET /v1.2/credits`).
       *
       *     On an endpoint that opens with **API key or client token.**, `scope_insufficient` means the token was minted without the scope named at the end of that endpoint’s description.
       */
      403: {
        headers: {
          [name: string]: unknown;
        };
        content: {
          "application/json": components["schemas"]["ApiError"];
        };
      };
      /** @description Balance reads are capped at 60 per minute by default (`code: rate_limited`), and that is the only limit on your account that applies here — neither the monthly credit allowance nor the shared request limit does, so this 429 is never `credit_limit_exceeded`. Honor `Retry-After`; the window reopens one minute after your first read in it, so a retry shortly afterwards succeeds. A burst of traffic from a single IP can also be refused by the service-wide throttle, likewise with `Retry-After`. */
      429: {
        headers: {
          /** @description Seconds to wait before retrying. */
          "Retry-After"?: string;
          [name: string]: unknown;
        };
        content: {
          "application/json": components["schemas"]["ApiError"];
        };
      };
      /** @description Any other error: the HTTP status plus { code, message }. Retry only rate_limited and the transient 5xx codes. */
      default: {
        headers: {
          [name: string]: unknown;
        };
        content: {
          "application/json": components["schemas"]["ApiError"];
        };
      };
    };
  };
  searchFoods: {
    parameters: {
      query: {
        /** @description The food name to search for. */
        query: string;
        /** @description Narrows results to one kind of food. Omitted, all three are searched and returned as one ranked list, so a partner who does not care which kind a match is does not have to ask three times. */
        type?: "generic" | "branded" | "recipe";
        /** @description Maximum number of results to return. */
        limit?: number;
      };
      header?: never;
      path?: never;
      cookie?: never;
    };
    requestBody?: never;
    responses: {
      /** @description Foods matching the query, best match first; empty when nothing matches. */
      200: {
        headers: {
          [name: string]: unknown;
        };
        content: {
          "application/json": components["schemas"]["FoodSearchResponse"];
        };
      };
      /** @description A parameter is missing or invalid; the message names the parameter and the accepted values. */
      400: {
        headers: {
          [name: string]: unknown;
        };
        content: {
          "application/json": components["schemas"]["ApiError"];
        };
      };
      /**
       * @description The request carried no `Authorization` header, or the credential in it was rejected.
       *
       *     `unauthorized` — the header is missing or malformed, or the key is not one we recognise. A valid key belonging to the other API version is `403 forbidden` instead.
       *
       *     A client token is rejected in one of three ways, and only the first should be handled automatically:
       *
       *     - `token_expired` — the token is past its TTL. Mint a fresh one from your backend and retry the request once. This is routine and expected once per TTL window.
       *     - `token_invalid` — no such token: it was never issued, or it has been purged, which happens shortly after it expires. This code is not an automatic token-refresh signal.
       *     - `token_revoked` — the token was revoked, by `POST /v1.2/auth/client-token-revocations` or from the dashboard. The end user signs in again in your app, and your backend decides whether to mint another; a device that just re-mints defeats the revocation.
       */
      401: {
        headers: {
          /** @description RFC 6750 challenge. `Bearer` when the request carried no credential; `Bearer error="invalid_token", error_description="token expired|token invalid|token revoked"` when one was rejected. */
          "WWW-Authenticate"?: string;
          [name: string]: unknown;
        };
        content: {
          "application/json": components["schemas"]["ApiError"];
        };
      };
      /**
       * @description The credential is valid but is not allowed to make this request. `forbidden` is the general case — a key issued for the other API version, for example. A client token adds `client_token_not_allowed`, meaning the endpoint takes only an `sk-` API key — its description opens with **API key only.** (in this API: `POST /v1.2/auth/client-tokens`, `POST /v1.2/auth/client-token-revocations`, `POST /v1.2/chat/completions`, and `GET /v1.2/credits`).
       *
       *     On an endpoint that opens with **API key or client token.**, `scope_insufficient` means the token was minted without the scope named at the end of that endpoint’s description.
       */
      403: {
        headers: {
          [name: string]: unknown;
        };
        content: {
          "application/json": components["schemas"]["ApiError"];
        };
      };
      /** @description Either a rate limit was exceeded (`code: rate_limited`) or the monthly credit allowance is spent (`code: credit_limit_exceeded`). When Retry-After is present, wait that many seconds; a per-day allowance resets 24 hours after the first request in its window. Credit exhaustion carries no Retry-After and retrying does not help — the allowance returns at the start of the next calendar month. Call `GET /v1.2/credits` for the balance and reset date. */
      429: {
        headers: {
          /** @description Seconds to wait before retrying; present when the window is known. */
          "Retry-After"?: string;
          [name: string]: unknown;
        };
        content: {
          "application/json": components["schemas"]["ApiError"];
        };
      };
      /** @description Any other error: the HTTP status plus { code, message }. Retry only rate_limited and the transient 5xx codes. */
      default: {
        headers: {
          [name: string]: unknown;
        };
        content: {
          "application/json": components["schemas"]["ApiError"];
        };
      };
    };
  };
  autocompleteFoods: {
    parameters: {
      query: {
        /** @description The characters the user has typed so far. Fewer than 2 letters or digits yield no suggestions. */
        query: string;
        /** @description Narrows suggestions to one kind of food. Omitted, generic and branded foods are suggested together, generic first. */
        type?: "generic" | "branded";
        /** @description Maximum number of suggestions to return. */
        limit?: number;
      };
      header?: never;
      path?: never;
      cookie?: never;
    };
    requestBody?: never;
    responses: {
      /** @description Type-ahead suggestions for the partial name, generic before branded; empty when fewer than 2 characters were given or nothing matches. */
      200: {
        headers: {
          [name: string]: unknown;
        };
        content: {
          "application/json": components["schemas"]["FoodSuggestionsResponse"];
        };
      };
      /** @description A parameter is missing or invalid; the message names the parameter and the accepted values. */
      400: {
        headers: {
          [name: string]: unknown;
        };
        content: {
          "application/json": components["schemas"]["ApiError"];
        };
      };
      /**
       * @description The request carried no `Authorization` header, or the credential in it was rejected.
       *
       *     `unauthorized` — the header is missing or malformed, or the key is not one we recognise. A valid key belonging to the other API version is `403 forbidden` instead.
       *
       *     A client token is rejected in one of three ways, and only the first should be handled automatically:
       *
       *     - `token_expired` — the token is past its TTL. Mint a fresh one from your backend and retry the request once. This is routine and expected once per TTL window.
       *     - `token_invalid` — no such token: it was never issued, or it has been purged, which happens shortly after it expires. This code is not an automatic token-refresh signal.
       *     - `token_revoked` — the token was revoked, by `POST /v1.2/auth/client-token-revocations` or from the dashboard. The end user signs in again in your app, and your backend decides whether to mint another; a device that just re-mints defeats the revocation.
       */
      401: {
        headers: {
          /** @description RFC 6750 challenge. `Bearer` when the request carried no credential; `Bearer error="invalid_token", error_description="token expired|token invalid|token revoked"` when one was rejected. */
          "WWW-Authenticate"?: string;
          [name: string]: unknown;
        };
        content: {
          "application/json": components["schemas"]["ApiError"];
        };
      };
      /**
       * @description The credential is valid but is not allowed to make this request. `forbidden` is the general case — a key issued for the other API version, for example. A client token adds `client_token_not_allowed`, meaning the endpoint takes only an `sk-` API key — its description opens with **API key only.** (in this API: `POST /v1.2/auth/client-tokens`, `POST /v1.2/auth/client-token-revocations`, `POST /v1.2/chat/completions`, and `GET /v1.2/credits`).
       *
       *     On an endpoint that opens with **API key or client token.**, `scope_insufficient` means the token was minted without the scope named at the end of that endpoint’s description.
       */
      403: {
        headers: {
          [name: string]: unknown;
        };
        content: {
          "application/json": components["schemas"]["ApiError"];
        };
      };
      /** @description Either a rate limit was exceeded (`code: rate_limited`) or the monthly credit allowance is spent (`code: credit_limit_exceeded`). When Retry-After is present, wait that many seconds; a per-day allowance resets 24 hours after the first request in its window. Credit exhaustion carries no Retry-After and retrying does not help — the allowance returns at the start of the next calendar month. Call `GET /v1.2/credits` for the balance and reset date. */
      429: {
        headers: {
          /** @description Seconds to wait before retrying; present when the window is known. */
          "Retry-After"?: string;
          [name: string]: unknown;
        };
        content: {
          "application/json": components["schemas"]["ApiError"];
        };
      };
      /** @description Any other error: the HTTP status plus { code, message }. Retry only rate_limited and the transient 5xx codes. */
      default: {
        headers: {
          [name: string]: unknown;
        };
        content: {
          "application/json": components["schemas"]["ApiError"];
        };
      };
    };
  };
  suggestFoodAlternatives: {
    parameters: {
      query?: never;
      header?: never;
      path: {
        /** @description Food id from a search or food-analysis result. The example is brown rice. */
        food_id: string;
      };
      cookie?: never;
    };
    requestBody: {
      content: {
        "application/json": components["schemas"]["SuggestFoodAlternativesRequest"];
      };
    };
    responses: {
      /** @description Healthier alternatives honoring the restrictions and preferences; may be empty. */
      200: {
        headers: {
          [name: string]: unknown;
        };
        content: {
          "application/json": components["schemas"]["FoodAlternatives"];
        };
      };
      /** @description A field is missing or a value is not in the allowed vocabulary; the message names it. */
      400: {
        headers: {
          [name: string]: unknown;
        };
        content: {
          "application/json": components["schemas"]["ApiError"];
        };
      };
      /**
       * @description The request carried no `Authorization` header, or the credential in it was rejected.
       *
       *     `unauthorized` — the header is missing or malformed, or the key is not one we recognise. A valid key belonging to the other API version is `403 forbidden` instead.
       *
       *     A client token is rejected in one of three ways, and only the first should be handled automatically:
       *
       *     - `token_expired` — the token is past its TTL. Mint a fresh one from your backend and retry the request once. This is routine and expected once per TTL window.
       *     - `token_invalid` — no such token: it was never issued, or it has been purged, which happens shortly after it expires. This code is not an automatic token-refresh signal.
       *     - `token_revoked` — the token was revoked, by `POST /v1.2/auth/client-token-revocations` or from the dashboard. The end user signs in again in your app, and your backend decides whether to mint another; a device that just re-mints defeats the revocation.
       */
      401: {
        headers: {
          /** @description RFC 6750 challenge. `Bearer` when the request carried no credential; `Bearer error="invalid_token", error_description="token expired|token invalid|token revoked"` when one was rejected. */
          "WWW-Authenticate"?: string;
          [name: string]: unknown;
        };
        content: {
          "application/json": components["schemas"]["ApiError"];
        };
      };
      /**
       * @description The credential is valid but is not allowed to make this request. `forbidden` is the general case — a key issued for the other API version, for example. A client token adds `client_token_not_allowed`, meaning the endpoint takes only an `sk-` API key — its description opens with **API key only.** (in this API: `POST /v1.2/auth/client-tokens`, `POST /v1.2/auth/client-token-revocations`, `POST /v1.2/chat/completions`, and `GET /v1.2/credits`).
       *
       *     On an endpoint that opens with **API key or client token.**, `scope_insufficient` means the token was minted without the scope named at the end of that endpoint’s description.
       */
      403: {
        headers: {
          [name: string]: unknown;
        };
        content: {
          "application/json": components["schemas"]["ApiError"];
        };
      };
      /** @description No food with this id. */
      404: {
        headers: {
          [name: string]: unknown;
        };
        content: {
          "application/json": components["schemas"]["ApiError"];
        };
      };
      /** @description Either a rate limit was exceeded (`code: rate_limited`) or the monthly credit allowance is spent (`code: credit_limit_exceeded`). When Retry-After is present, wait that many seconds; a per-day allowance resets 24 hours after the first request in its window. Credit exhaustion carries no Retry-After and retrying does not help — the allowance returns at the start of the next calendar month. Call `GET /v1.2/credits` for the balance and reset date. */
      429: {
        headers: {
          /** @description Seconds to wait before retrying; present when the window is known. */
          "Retry-After"?: string;
          [name: string]: unknown;
        };
        content: {
          "application/json": components["schemas"]["ApiError"];
        };
      };
      /** @description Any other error: the HTTP status plus { code, message }. Retry only rate_limited and the transient 5xx codes. */
      default: {
        headers: {
          [name: string]: unknown;
        };
        content: {
          "application/json": components["schemas"]["ApiError"];
        };
      };
    };
  };
  getFoodByBarcode: {
    parameters: {
      query?: never;
      header?: never;
      path: {
        /** @description The numeric barcode: 6 to 14 digits (UPC-E, UPC-A, EAN-8, EAN-13 or GTIN-14). The example is a Coca-Cola can. */
        barcode: string;
      };
      cookie?: never;
    };
    requestBody?: never;
    responses: {
      /** @description The single food the barcode identifies. */
      200: {
        headers: {
          [name: string]: unknown;
        };
        content: {
          "application/json": components["schemas"]["Food"];
        };
      };
      /** @description The barcode is not a 6 to 14 digit number. */
      400: {
        headers: {
          [name: string]: unknown;
        };
        content: {
          "application/json": components["schemas"]["ApiError"];
        };
      };
      /**
       * @description The request carried no `Authorization` header, or the credential in it was rejected.
       *
       *     `unauthorized` — the header is missing or malformed, or the key is not one we recognise. A valid key belonging to the other API version is `403 forbidden` instead.
       *
       *     A client token is rejected in one of three ways, and only the first should be handled automatically:
       *
       *     - `token_expired` — the token is past its TTL. Mint a fresh one from your backend and retry the request once. This is routine and expected once per TTL window.
       *     - `token_invalid` — no such token: it was never issued, or it has been purged, which happens shortly after it expires. This code is not an automatic token-refresh signal.
       *     - `token_revoked` — the token was revoked, by `POST /v1.2/auth/client-token-revocations` or from the dashboard. The end user signs in again in your app, and your backend decides whether to mint another; a device that just re-mints defeats the revocation.
       */
      401: {
        headers: {
          /** @description RFC 6750 challenge. `Bearer` when the request carried no credential; `Bearer error="invalid_token", error_description="token expired|token invalid|token revoked"` when one was rejected. */
          "WWW-Authenticate"?: string;
          [name: string]: unknown;
        };
        content: {
          "application/json": components["schemas"]["ApiError"];
        };
      };
      /**
       * @description The credential is valid but is not allowed to make this request. `forbidden` is the general case — a key issued for the other API version, for example. A client token adds `client_token_not_allowed`, meaning the endpoint takes only an `sk-` API key — its description opens with **API key only.** (in this API: `POST /v1.2/auth/client-tokens`, `POST /v1.2/auth/client-token-revocations`, `POST /v1.2/chat/completions`, and `GET /v1.2/credits`).
       *
       *     On an endpoint that opens with **API key or client token.**, `scope_insufficient` means the token was minted without the scope named at the end of that endpoint’s description.
       */
      403: {
        headers: {
          [name: string]: unknown;
        };
        content: {
          "application/json": components["schemas"]["ApiError"];
        };
      };
      /** @description No food matches this barcode. */
      404: {
        headers: {
          [name: string]: unknown;
        };
        content: {
          "application/json": components["schemas"]["ApiError"];
        };
      };
      /** @description Either a rate limit was exceeded (`code: rate_limited`) or the monthly credit allowance is spent (`code: credit_limit_exceeded`). When Retry-After is present, wait that many seconds; a per-day allowance resets 24 hours after the first request in its window. Credit exhaustion carries no Retry-After and retrying does not help — the allowance returns at the start of the next calendar month. Call `GET /v1.2/credits` for the balance and reset date. */
      429: {
        headers: {
          /** @description Seconds to wait before retrying; present when the window is known. */
          "Retry-After"?: string;
          [name: string]: unknown;
        };
        content: {
          "application/json": components["schemas"]["ApiError"];
        };
      };
      /** @description Any other error: the HTTP status plus { code, message }. Retry only rate_limited and the transient 5xx codes. */
      default: {
        headers: {
          [name: string]: unknown;
        };
        content: {
          "application/json": components["schemas"]["ApiError"];
        };
      };
    };
  };
  getFood: {
    parameters: {
      query?: never;
      header?: never;
      path: {
        /** @description Food id from a search or food-analysis result. */
        food_id: string;
      };
      cookie?: never;
    };
    requestBody?: never;
    responses: {
      /** @description The food's full record, including the complete list of serving sizes. */
      200: {
        headers: {
          [name: string]: unknown;
        };
        content: {
          "application/json": components["schemas"]["Food"];
        };
      };
      /** @description food_id is not a numeric id; the message shows the expected form. */
      400: {
        headers: {
          [name: string]: unknown;
        };
        content: {
          "application/json": components["schemas"]["ApiError"];
        };
      };
      /**
       * @description The request carried no `Authorization` header, or the credential in it was rejected.
       *
       *     `unauthorized` — the header is missing or malformed, or the key is not one we recognise. A valid key belonging to the other API version is `403 forbidden` instead.
       *
       *     A client token is rejected in one of three ways, and only the first should be handled automatically:
       *
       *     - `token_expired` — the token is past its TTL. Mint a fresh one from your backend and retry the request once. This is routine and expected once per TTL window.
       *     - `token_invalid` — no such token: it was never issued, or it has been purged, which happens shortly after it expires. This code is not an automatic token-refresh signal.
       *     - `token_revoked` — the token was revoked, by `POST /v1.2/auth/client-token-revocations` or from the dashboard. The end user signs in again in your app, and your backend decides whether to mint another; a device that just re-mints defeats the revocation.
       */
      401: {
        headers: {
          /** @description RFC 6750 challenge. `Bearer` when the request carried no credential; `Bearer error="invalid_token", error_description="token expired|token invalid|token revoked"` when one was rejected. */
          "WWW-Authenticate"?: string;
          [name: string]: unknown;
        };
        content: {
          "application/json": components["schemas"]["ApiError"];
        };
      };
      /**
       * @description The credential is valid but is not allowed to make this request. `forbidden` is the general case — a key issued for the other API version, for example. A client token adds `client_token_not_allowed`, meaning the endpoint takes only an `sk-` API key — its description opens with **API key only.** (in this API: `POST /v1.2/auth/client-tokens`, `POST /v1.2/auth/client-token-revocations`, `POST /v1.2/chat/completions`, and `GET /v1.2/credits`).
       *
       *     On an endpoint that opens with **API key or client token.**, `scope_insufficient` means the token was minted without the scope named at the end of that endpoint’s description.
       */
      403: {
        headers: {
          [name: string]: unknown;
        };
        content: {
          "application/json": components["schemas"]["ApiError"];
        };
      };
      /** @description No food with this id. */
      404: {
        headers: {
          [name: string]: unknown;
        };
        content: {
          "application/json": components["schemas"]["ApiError"];
        };
      };
      /** @description Either a rate limit was exceeded (`code: rate_limited`) or the monthly credit allowance is spent (`code: credit_limit_exceeded`). When Retry-After is present, wait that many seconds; a per-day allowance resets 24 hours after the first request in its window. Credit exhaustion carries no Retry-After and retrying does not help — the allowance returns at the start of the next calendar month. Call `GET /v1.2/credits` for the balance and reset date. */
      429: {
        headers: {
          /** @description Seconds to wait before retrying; present when the window is known. */
          "Retry-After"?: string;
          [name: string]: unknown;
        };
        content: {
          "application/json": components["schemas"]["ApiError"];
        };
      };
      /** @description Any other error: the HTTP status plus { code, message }. Retry only rate_limited and the transient 5xx codes. */
      default: {
        headers: {
          [name: string]: unknown;
        };
        content: {
          "application/json": components["schemas"]["ApiError"];
        };
      };
    };
  };
  searchRestaurants: {
    parameters: {
      query: {
        /** @description Search radius in meters around (latitude, longitude), e.g. 5000 = 5 kilometers. A fractional value is accepted and rounded. Default 8000 meters (about 5 miles); maximum 50000 (about 31 miles). */
        radius_meters?: number;
        /** @description Maximum number of results to return. */
        limit?: number;
        /** @description Restaurant name to search for. */
        query: string;
        /** @description Latitude of the search location, e.g. 37.7749 (San Francisco). */
        latitude: number;
        /** @description Longitude of the search location, e.g. -122.4194 (San Francisco). */
        longitude: number;
      };
      header?: never;
      path?: never;
      cookie?: never;
    };
    requestBody?: never;
    responses: {
      /** @description Restaurants matching the query near the location, ranked by proximity. Empty when nothing matches. */
      200: {
        headers: {
          [name: string]: unknown;
        };
        content: {
          "application/json": components["schemas"]["RestaurantSearchResponse"];
        };
      };
      /** @description A parameter is missing or invalid; the message names the parameter and the accepted values. */
      400: {
        headers: {
          [name: string]: unknown;
        };
        content: {
          "application/json": components["schemas"]["ApiError"];
        };
      };
      /**
       * @description The request carried no `Authorization` header, or the credential in it was rejected.
       *
       *     `unauthorized` — the header is missing or malformed, or the key is not one we recognise. A valid key belonging to the other API version is `403 forbidden` instead.
       *
       *     A client token is rejected in one of three ways, and only the first should be handled automatically:
       *
       *     - `token_expired` — the token is past its TTL. Mint a fresh one from your backend and retry the request once. This is routine and expected once per TTL window.
       *     - `token_invalid` — no such token: it was never issued, or it has been purged, which happens shortly after it expires. This code is not an automatic token-refresh signal.
       *     - `token_revoked` — the token was revoked, by `POST /v1.2/auth/client-token-revocations` or from the dashboard. The end user signs in again in your app, and your backend decides whether to mint another; a device that just re-mints defeats the revocation.
       */
      401: {
        headers: {
          /** @description RFC 6750 challenge. `Bearer` when the request carried no credential; `Bearer error="invalid_token", error_description="token expired|token invalid|token revoked"` when one was rejected. */
          "WWW-Authenticate"?: string;
          [name: string]: unknown;
        };
        content: {
          "application/json": components["schemas"]["ApiError"];
        };
      };
      /**
       * @description The credential is valid but is not allowed to make this request. `forbidden` is the general case — a key issued for the other API version, for example. A client token adds `client_token_not_allowed`, meaning the endpoint takes only an `sk-` API key — its description opens with **API key only.** (in this API: `POST /v1.2/auth/client-tokens`, `POST /v1.2/auth/client-token-revocations`, `POST /v1.2/chat/completions`, and `GET /v1.2/credits`).
       *
       *     On an endpoint that opens with **API key or client token.**, `scope_insufficient` means the token was minted without the scope named at the end of that endpoint’s description.
       */
      403: {
        headers: {
          [name: string]: unknown;
        };
        content: {
          "application/json": components["schemas"]["ApiError"];
        };
      };
      /** @description Either a rate limit was exceeded (`code: rate_limited`) or the monthly credit allowance is spent (`code: credit_limit_exceeded`). When Retry-After is present, wait that many seconds; a per-day allowance resets 24 hours after the first request in its window. Credit exhaustion carries no Retry-After and retrying does not help — the allowance returns at the start of the next calendar month. Call `GET /v1.2/credits` for the balance and reset date. */
      429: {
        headers: {
          /** @description Seconds to wait before retrying; present when the window is known. */
          "Retry-After"?: string;
          [name: string]: unknown;
        };
        content: {
          "application/json": components["schemas"]["ApiError"];
        };
      };
      /** @description Any other error: the HTTP status plus { code, message }. Retry only rate_limited and the transient 5xx codes. */
      default: {
        headers: {
          [name: string]: unknown;
        };
        content: {
          "application/json": components["schemas"]["ApiError"];
        };
      };
    };
  };
  listRestaurantMenuItems: {
    parameters: {
      query?: {
        /** @description Maximum number of menu items to return. Raise it, or page with `offset`, for a long menu. */
        limit?: number;
        /** @description Number of menu items to skip, for paging: a page shorter than `limit` is the last one. */
        offset?: number;
      };
      header?: never;
      path: {
        /** @description Restaurant id from a `GET /v1.2/restaurants` result. */
        restaurant_id: string;
      };
      cookie?: never;
    };
    requestBody?: never;
    responses: {
      /** @description The restaurant's menu items, by name. Empty when the restaurant has no menu on record or `offset` is past its end. */
      200: {
        headers: {
          [name: string]: unknown;
        };
        content: {
          "application/json": components["schemas"]["RestaurantMenuItemsResponse"];
        };
      };
      /** @description restaurant_id, limit or offset is malformed; the message names the parameter and the accepted values. */
      400: {
        headers: {
          [name: string]: unknown;
        };
        content: {
          "application/json": components["schemas"]["ApiError"];
        };
      };
      /**
       * @description The request carried no `Authorization` header, or the credential in it was rejected.
       *
       *     `unauthorized` — the header is missing or malformed, or the key is not one we recognise. A valid key belonging to the other API version is `403 forbidden` instead.
       *
       *     A client token is rejected in one of three ways, and only the first should be handled automatically:
       *
       *     - `token_expired` — the token is past its TTL. Mint a fresh one from your backend and retry the request once. This is routine and expected once per TTL window.
       *     - `token_invalid` — no such token: it was never issued, or it has been purged, which happens shortly after it expires. This code is not an automatic token-refresh signal.
       *     - `token_revoked` — the token was revoked, by `POST /v1.2/auth/client-token-revocations` or from the dashboard. The end user signs in again in your app, and your backend decides whether to mint another; a device that just re-mints defeats the revocation.
       */
      401: {
        headers: {
          /** @description RFC 6750 challenge. `Bearer` when the request carried no credential; `Bearer error="invalid_token", error_description="token expired|token invalid|token revoked"` when one was rejected. */
          "WWW-Authenticate"?: string;
          [name: string]: unknown;
        };
        content: {
          "application/json": components["schemas"]["ApiError"];
        };
      };
      /**
       * @description The credential is valid but is not allowed to make this request. `forbidden` is the general case — a key issued for the other API version, for example. A client token adds `client_token_not_allowed`, meaning the endpoint takes only an `sk-` API key — its description opens with **API key only.** (in this API: `POST /v1.2/auth/client-tokens`, `POST /v1.2/auth/client-token-revocations`, `POST /v1.2/chat/completions`, and `GET /v1.2/credits`).
       *
       *     On an endpoint that opens with **API key or client token.**, `scope_insufficient` means the token was minted without the scope named at the end of that endpoint’s description.
       */
      403: {
        headers: {
          [name: string]: unknown;
        };
        content: {
          "application/json": components["schemas"]["ApiError"];
        };
      };
      /** @description No restaurant with this id. */
      404: {
        headers: {
          [name: string]: unknown;
        };
        content: {
          "application/json": components["schemas"]["ApiError"];
        };
      };
      /** @description Either a rate limit was exceeded (`code: rate_limited`) or the monthly credit allowance is spent (`code: credit_limit_exceeded`). When Retry-After is present, wait that many seconds; a per-day allowance resets 24 hours after the first request in its window. Credit exhaustion carries no Retry-After and retrying does not help — the allowance returns at the start of the next calendar month. Call `GET /v1.2/credits` for the balance and reset date. */
      429: {
        headers: {
          /** @description Seconds to wait before retrying; present when the window is known. */
          "Retry-After"?: string;
          [name: string]: unknown;
        };
        content: {
          "application/json": components["schemas"]["ApiError"];
        };
      };
      /** @description Any other error: the HTTP status plus { code, message }. Retry only rate_limited and the transient 5xx codes. */
      default: {
        headers: {
          [name: string]: unknown;
        };
        content: {
          "application/json": components["schemas"]["ApiError"];
        };
      };
    };
  };
  searchMenuItems: {
    parameters: {
      query: {
        /** @description Search radius in meters around (latitude, longitude), e.g. 5000 = 5 kilometers. A fractional value is accepted and rounded. Default 8000 meters (about 5 miles); maximum 50000 (about 31 miles). */
        radius_meters?: number;
        /** @description Maximum number of results to return. */
        limit?: number;
        /** @description Dish or restaurant name to search for. */
        query: string;
        /** @description Latitude of the search location, e.g. 37.7749 (San Francisco). */
        latitude: number;
        /** @description Longitude of the search location, e.g. -122.4194 (San Francisco). */
        longitude: number;
      };
      header?: never;
      path?: never;
      cookie?: never;
    };
    requestBody?: never;
    responses: {
      /** @description Dishes near the location matching the query, ranked by proximity, each with the nutrition its source publishes. Empty when nothing matches. */
      200: {
        headers: {
          [name: string]: unknown;
        };
        content: {
          "application/json": components["schemas"]["MenuItemSearchResponse"];
        };
      };
      /** @description A parameter is missing or invalid; the message names the parameter and the accepted values. */
      400: {
        headers: {
          [name: string]: unknown;
        };
        content: {
          "application/json": components["schemas"]["ApiError"];
        };
      };
      /**
       * @description The request carried no `Authorization` header, or the credential in it was rejected.
       *
       *     `unauthorized` — the header is missing or malformed, or the key is not one we recognise. A valid key belonging to the other API version is `403 forbidden` instead.
       *
       *     A client token is rejected in one of three ways, and only the first should be handled automatically:
       *
       *     - `token_expired` — the token is past its TTL. Mint a fresh one from your backend and retry the request once. This is routine and expected once per TTL window.
       *     - `token_invalid` — no such token: it was never issued, or it has been purged, which happens shortly after it expires. This code is not an automatic token-refresh signal.
       *     - `token_revoked` — the token was revoked, by `POST /v1.2/auth/client-token-revocations` or from the dashboard. The end user signs in again in your app, and your backend decides whether to mint another; a device that just re-mints defeats the revocation.
       */
      401: {
        headers: {
          /** @description RFC 6750 challenge. `Bearer` when the request carried no credential; `Bearer error="invalid_token", error_description="token expired|token invalid|token revoked"` when one was rejected. */
          "WWW-Authenticate"?: string;
          [name: string]: unknown;
        };
        content: {
          "application/json": components["schemas"]["ApiError"];
        };
      };
      /**
       * @description The credential is valid but is not allowed to make this request. `forbidden` is the general case — a key issued for the other API version, for example. A client token adds `client_token_not_allowed`, meaning the endpoint takes only an `sk-` API key — its description opens with **API key only.** (in this API: `POST /v1.2/auth/client-tokens`, `POST /v1.2/auth/client-token-revocations`, `POST /v1.2/chat/completions`, and `GET /v1.2/credits`).
       *
       *     On an endpoint that opens with **API key or client token.**, `scope_insufficient` means the token was minted without the scope named at the end of that endpoint’s description.
       */
      403: {
        headers: {
          [name: string]: unknown;
        };
        content: {
          "application/json": components["schemas"]["ApiError"];
        };
      };
      /** @description Either a rate limit was exceeded (`code: rate_limited`) or the monthly credit allowance is spent (`code: credit_limit_exceeded`). When Retry-After is present, wait that many seconds; a per-day allowance resets 24 hours after the first request in its window. Credit exhaustion carries no Retry-After and retrying does not help — the allowance returns at the start of the next calendar month. Call `GET /v1.2/credits` for the balance and reset date. */
      429: {
        headers: {
          /** @description Seconds to wait before retrying; present when the window is known. */
          "Retry-After"?: string;
          [name: string]: unknown;
        };
        content: {
          "application/json": components["schemas"]["ApiError"];
        };
      };
      /** @description Any other error: the HTTP status plus { code, message }. Retry only rate_limited and the transient 5xx codes. */
      default: {
        headers: {
          [name: string]: unknown;
        };
        content: {
          "application/json": components["schemas"]["ApiError"];
        };
      };
    };
  };
  analyzeFoodImage: {
    parameters: {
      query?: never;
      header?: never;
      path?: never;
      cookie?: never;
    };
    requestBody: {
      content: {
        "application/json": components["schemas"]["ImageFoodAnalysisRequest"];
      };
    };
    responses: {
      /** @description The foods detected in the photo, with per-item and total nutrition. */
      200: {
        headers: {
          [name: string]: unknown;
        };
        content: {
          "application/json": components["schemas"]["FoodAnalysisResult"];
        };
      };
      /** @description `invalid_request`: the image is missing or not an http(s) URL / data URI, or the photo shows nothing but a barcode. `image_unreachable`, `image_corrupt`, `image_format_unsupported`, `image_invalid_base64`: the image itself could not be used — the message says what to fix, and retrying the same image fails the same way. */
      400: {
        headers: {
          [name: string]: unknown;
        };
        content: {
          "application/json": components["schemas"]["ApiError"];
        };
      };
      /**
       * @description The request carried no `Authorization` header, or the credential in it was rejected.
       *
       *     `unauthorized` — the header is missing or malformed, or the key is not one we recognise. A valid key belonging to the other API version is `403 forbidden` instead.
       *
       *     A client token is rejected in one of three ways, and only the first should be handled automatically:
       *
       *     - `token_expired` — the token is past its TTL. Mint a fresh one from your backend and retry the request once. This is routine and expected once per TTL window.
       *     - `token_invalid` — no such token: it was never issued, or it has been purged, which happens shortly after it expires. This code is not an automatic token-refresh signal.
       *     - `token_revoked` — the token was revoked, by `POST /v1.2/auth/client-token-revocations` or from the dashboard. The end user signs in again in your app, and your backend decides whether to mint another; a device that just re-mints defeats the revocation.
       */
      401: {
        headers: {
          /** @description RFC 6750 challenge. `Bearer` when the request carried no credential; `Bearer error="invalid_token", error_description="token expired|token invalid|token revoked"` when one was rejected. */
          "WWW-Authenticate"?: string;
          [name: string]: unknown;
        };
        content: {
          "application/json": components["schemas"]["ApiError"];
        };
      };
      /**
       * @description The credential is valid but is not allowed to make this request. `forbidden` is the general case — a key issued for the other API version, for example. A client token adds `client_token_not_allowed`, meaning the endpoint takes only an `sk-` API key — its description opens with **API key only.** (in this API: `POST /v1.2/auth/client-tokens`, `POST /v1.2/auth/client-token-revocations`, `POST /v1.2/chat/completions`, and `GET /v1.2/credits`).
       *
       *     On an endpoint that opens with **API key or client token.**, `scope_insufficient` means the token was minted without the scope named at the end of that endpoint’s description.
       */
      403: {
        headers: {
          [name: string]: unknown;
        };
        content: {
          "application/json": components["schemas"]["ApiError"];
        };
      };
      /** @description The request body exceeds 5 MB (a transport cap, not an image-quality limit). Keep raw images under ~3.5 MB before base64-encoding, or send a URL instead. */
      413: {
        headers: {
          [name: string]: unknown;
        };
        content: {
          "application/json": components["schemas"]["ApiError"];
        };
      };
      /** @description Either a rate limit was exceeded (`code: rate_limited`) or the monthly credit allowance is spent (`code: credit_limit_exceeded`). When Retry-After is present, wait that many seconds; a per-day allowance resets 24 hours after the first request in its window. Credit exhaustion carries no Retry-After and retrying does not help — the allowance returns at the start of the next calendar month. Call `GET /v1.2/credits` for the balance and reset date. */
      429: {
        headers: {
          /** @description Seconds to wait before retrying; present when the window is known. */
          "Retry-After"?: string;
          [name: string]: unknown;
        };
        content: {
          "application/json": components["schemas"]["ApiError"];
        };
      };
      /** @description The vision model took too long; retry, ideally with a smaller image. */
      504: {
        headers: {
          [name: string]: unknown;
        };
        content: {
          "application/json": components["schemas"]["ApiError"];
        };
      };
      /** @description Any other error: the HTTP status plus { code, message }. Retry only rate_limited and the transient 5xx codes. */
      default: {
        headers: {
          [name: string]: unknown;
        };
        content: {
          "application/json": components["schemas"]["ApiError"];
        };
      };
    };
  };
  analyzeFoodText: {
    parameters: {
      query?: never;
      header?: never;
      path?: never;
      cookie?: never;
    };
    requestBody: {
      content: {
        "application/json": components["schemas"]["TextFoodAnalysisRequest"];
      };
    };
    responses: {
      /** @description The foods detected in the text, with per-item and total nutrition. */
      200: {
        headers: {
          [name: string]: unknown;
        };
        content: {
          "application/json": components["schemas"]["FoodAnalysisResult"];
        };
      };
      /** @description `invalid_request`: the text is missing or exceeds 512 characters. */
      400: {
        headers: {
          [name: string]: unknown;
        };
        content: {
          "application/json": components["schemas"]["ApiError"];
        };
      };
      /**
       * @description The request carried no `Authorization` header, or the credential in it was rejected.
       *
       *     `unauthorized` — the header is missing or malformed, or the key is not one we recognise. A valid key belonging to the other API version is `403 forbidden` instead.
       *
       *     A client token is rejected in one of three ways, and only the first should be handled automatically:
       *
       *     - `token_expired` — the token is past its TTL. Mint a fresh one from your backend and retry the request once. This is routine and expected once per TTL window.
       *     - `token_invalid` — no such token: it was never issued, or it has been purged, which happens shortly after it expires. This code is not an automatic token-refresh signal.
       *     - `token_revoked` — the token was revoked, by `POST /v1.2/auth/client-token-revocations` or from the dashboard. The end user signs in again in your app, and your backend decides whether to mint another; a device that just re-mints defeats the revocation.
       */
      401: {
        headers: {
          /** @description RFC 6750 challenge. `Bearer` when the request carried no credential; `Bearer error="invalid_token", error_description="token expired|token invalid|token revoked"` when one was rejected. */
          "WWW-Authenticate"?: string;
          [name: string]: unknown;
        };
        content: {
          "application/json": components["schemas"]["ApiError"];
        };
      };
      /**
       * @description The credential is valid but is not allowed to make this request. `forbidden` is the general case — a key issued for the other API version, for example. A client token adds `client_token_not_allowed`, meaning the endpoint takes only an `sk-` API key — its description opens with **API key only.** (in this API: `POST /v1.2/auth/client-tokens`, `POST /v1.2/auth/client-token-revocations`, `POST /v1.2/chat/completions`, and `GET /v1.2/credits`).
       *
       *     On an endpoint that opens with **API key or client token.**, `scope_insufficient` means the token was minted without the scope named at the end of that endpoint’s description.
       */
      403: {
        headers: {
          [name: string]: unknown;
        };
        content: {
          "application/json": components["schemas"]["ApiError"];
        };
      };
      /** @description Either a rate limit was exceeded (`code: rate_limited`) or the monthly credit allowance is spent (`code: credit_limit_exceeded`). When Retry-After is present, wait that many seconds; a per-day allowance resets 24 hours after the first request in its window. Credit exhaustion carries no Retry-After and retrying does not help — the allowance returns at the start of the next calendar month. Call `GET /v1.2/credits` for the balance and reset date. */
      429: {
        headers: {
          /** @description Seconds to wait before retrying; present when the window is known. */
          "Retry-After"?: string;
          [name: string]: unknown;
        };
        content: {
          "application/json": components["schemas"]["ApiError"];
        };
      };
      /** @description Any other error: the HTTP status plus { code, message }. Retry only rate_limited and the transient 5xx codes. */
      default: {
        headers: {
          [name: string]: unknown;
        };
        content: {
          "application/json": components["schemas"]["ApiError"];
        };
      };
    };
  };
  correctFoodAnalysis: {
    parameters: {
      query?: never;
      header?: never;
      path?: never;
      cookie?: never;
    };
    requestBody: {
      content: {
        "application/json": components["schemas"]["CorrectFoodAnalysisRequest"];
      };
    };
    responses: {
      /** @description The corrected analysis, with recalculated per-item and total nutrition. */
      200: {
        headers: {
          [name: string]: unknown;
        };
        content: {
          "application/json": components["schemas"]["FoodAnalysisResult"];
        };
      };
      /** @description A field is missing or a detection is incomplete; the message names the exact detection index and problem. */
      400: {
        headers: {
          [name: string]: unknown;
        };
        content: {
          "application/json": components["schemas"]["ApiError"];
        };
      };
      /**
       * @description The request carried no `Authorization` header, or the credential in it was rejected.
       *
       *     `unauthorized` — the header is missing or malformed, or the key is not one we recognise. A valid key belonging to the other API version is `403 forbidden` instead.
       *
       *     A client token is rejected in one of three ways, and only the first should be handled automatically:
       *
       *     - `token_expired` — the token is past its TTL. Mint a fresh one from your backend and retry the request once. This is routine and expected once per TTL window.
       *     - `token_invalid` — no such token: it was never issued, or it has been purged, which happens shortly after it expires. This code is not an automatic token-refresh signal.
       *     - `token_revoked` — the token was revoked, by `POST /v1.2/auth/client-token-revocations` or from the dashboard. The end user signs in again in your app, and your backend decides whether to mint another; a device that just re-mints defeats the revocation.
       */
      401: {
        headers: {
          /** @description RFC 6750 challenge. `Bearer` when the request carried no credential; `Bearer error="invalid_token", error_description="token expired|token invalid|token revoked"` when one was rejected. */
          "WWW-Authenticate"?: string;
          [name: string]: unknown;
        };
        content: {
          "application/json": components["schemas"]["ApiError"];
        };
      };
      /**
       * @description The credential is valid but is not allowed to make this request. `forbidden` is the general case — a key issued for the other API version, for example. A client token adds `client_token_not_allowed`, meaning the endpoint takes only an `sk-` API key — its description opens with **API key only.** (in this API: `POST /v1.2/auth/client-tokens`, `POST /v1.2/auth/client-token-revocations`, `POST /v1.2/chat/completions`, and `GET /v1.2/credits`).
       *
       *     On an endpoint that opens with **API key or client token.**, `scope_insufficient` means the token was minted without the scope named at the end of that endpoint’s description.
       */
      403: {
        headers: {
          [name: string]: unknown;
        };
        content: {
          "application/json": components["schemas"]["ApiError"];
        };
      };
      /** @description Either a rate limit was exceeded (`code: rate_limited`) or the monthly credit allowance is spent (`code: credit_limit_exceeded`). When Retry-After is present, wait that many seconds; a per-day allowance resets 24 hours after the first request in its window. Credit exhaustion carries no Retry-After and retrying does not help — the allowance returns at the start of the next calendar month. Call `GET /v1.2/credits` for the balance and reset date. */
      429: {
        headers: {
          /** @description Seconds to wait before retrying; present when the window is known. */
          "Retry-After"?: string;
          [name: string]: unknown;
        };
        content: {
          "application/json": components["schemas"]["ApiError"];
        };
      };
      /** @description The vision model took too long; retry. */
      504: {
        headers: {
          [name: string]: unknown;
        };
        content: {
          "application/json": components["schemas"]["ApiError"];
        };
      };
      /** @description Any other error: the HTTP status plus { code, message }. Retry only rate_limited and the transient 5xx codes. */
      default: {
        headers: {
          [name: string]: unknown;
        };
        content: {
          "application/json": components["schemas"]["ApiError"];
        };
      };
    };
  };
  listFoodLogs: {
    parameters: {
      query: {
        /** @description First local calendar date in `timezone`, inclusive. */
        start_date: string;
        /** @description Last local calendar date in `timezone`, inclusive. May equal start_date for a single day. The inclusive range may not exceed 60 calendar days. */
        end_date: string;
        /** @description IANA timezone that defines the local calendar days this range covers — required, so the upstream groups by the same days the caller means. */
        timezone: string;
      };
      header?: {
        /**
         * @description Your stable ID for the end user whose food logs this request reads or writes. Opaque to January — use the same ID your system already uses for them.
         *
         *     | Credential | Header | Result |
         *     | --- | --- | --- |
         *     | API key (`sk-…`) | absent | `400 end_user_id_required` |
         *     | API key (`sk-…`) | present | the request acts on that end user |
         *     | Client token (`ct-…`) | absent | filled in from the token |
         *     | Client token (`ct-…`) | the end user the token is bound to | accepted |
         *     | Client token (`ct-…`) | any other end user | `403 end_user_id_mismatch` |
         */
        "January-End-User-ID"?: components["parameters"]["JanuaryEndUserId"];
      };
      path?: never;
      cookie?: never;
    };
    requestBody?: never;
    responses: {
      /** @description The logs in the requested date range, ordered by timestamp; an empty array is a valid result. */
      200: {
        headers: {
          [name: string]: unknown;
        };
        content: {
          "application/json": components["schemas"]["FoodLogList"];
        };
      };
      /** @description A date is missing, malformed, or the range is inverted or exceeds 60 days; or timezone is missing or not a valid IANA name. */
      400: {
        headers: {
          [name: string]: unknown;
        };
        content: {
          "application/json": components["schemas"]["ApiError"];
        };
      };
      /**
       * @description The request carried no `Authorization` header, or the credential in it was rejected.
       *
       *     `unauthorized` — the header is missing or malformed, or the key is not one we recognise. A valid key belonging to the other API version is `403 forbidden` instead.
       *
       *     A client token is rejected in one of three ways, and only the first should be handled automatically:
       *
       *     - `token_expired` — the token is past its TTL. Mint a fresh one from your backend and retry the request once. This is routine and expected once per TTL window.
       *     - `token_invalid` — no such token: it was never issued, or it has been purged, which happens shortly after it expires. This code is not an automatic token-refresh signal.
       *     - `token_revoked` — the token was revoked, by `POST /v1.2/auth/client-token-revocations` or from the dashboard. The end user signs in again in your app, and your backend decides whether to mint another; a device that just re-mints defeats the revocation.
       */
      401: {
        headers: {
          /** @description RFC 6750 challenge. `Bearer` when the request carried no credential; `Bearer error="invalid_token", error_description="token expired|token invalid|token revoked"` when one was rejected. */
          "WWW-Authenticate"?: string;
          [name: string]: unknown;
        };
        content: {
          "application/json": components["schemas"]["ApiError"];
        };
      };
      /**
       * @description The credential is valid but is not allowed to make this request. `forbidden` is the general case — a key issued for the other API version, for example. A client token adds `client_token_not_allowed`, meaning the endpoint takes only an `sk-` API key — its description opens with **API key only.** (in this API: `POST /v1.2/auth/client-tokens`, `POST /v1.2/auth/client-token-revocations`, `POST /v1.2/chat/completions`, and `GET /v1.2/credits`).
       *
       *     On an endpoint that opens with **API key or client token.**, `scope_insufficient` means the token was minted without the scope named at the end of that endpoint’s description, and `end_user_id_mismatch` means the `January-End-User-ID` header disagrees with the end user the token is bound to — omit it, or send exactly that id.
       */
      403: {
        headers: {
          [name: string]: unknown;
        };
        content: {
          "application/json": components["schemas"]["ApiError"];
        };
      };
      /** @description Either a rate limit was exceeded (`code: rate_limited`) or the monthly credit allowance is spent (`code: credit_limit_exceeded`). When Retry-After is present, wait that many seconds; a per-day allowance resets 24 hours after the first request in its window. Credit exhaustion carries no Retry-After and retrying does not help — the allowance returns at the start of the next calendar month. Call `GET /v1.2/credits` for the balance and reset date. */
      429: {
        headers: {
          /** @description Seconds to wait before retrying; present when the window is known. */
          "Retry-After"?: string;
          [name: string]: unknown;
        };
        content: {
          "application/json": components["schemas"]["ApiError"];
        };
      };
      /** @description Any other error: the HTTP status plus { code, message }. Retry only rate_limited and the transient 5xx codes. */
      default: {
        headers: {
          [name: string]: unknown;
        };
        content: {
          "application/json": components["schemas"]["ApiError"];
        };
      };
    };
  };
  createFoodLog: {
    parameters: {
      query?: never;
      header?: {
        /**
         * @description Your stable ID for the end user whose food logs this request reads or writes. Opaque to January — use the same ID your system already uses for them.
         *
         *     | Credential | Header | Result |
         *     | --- | --- | --- |
         *     | API key (`sk-…`) | absent | `400 end_user_id_required` |
         *     | API key (`sk-…`) | present | the request acts on that end user |
         *     | Client token (`ct-…`) | absent | filled in from the token |
         *     | Client token (`ct-…`) | the end user the token is bound to | accepted |
         *     | Client token (`ct-…`) | any other end user | `403 end_user_id_mismatch` |
         */
        "January-End-User-ID"?: components["parameters"]["JanuaryEndUserId"];
      };
      path?: never;
      cookie?: never;
    };
    requestBody: {
      content: {
        "application/json": components["schemas"]["CreateFoodLogRequest"];
      };
    };
    responses: {
      /** @description The created log, hydrated with resolved food and nutrition; its id is in the Location header. */
      201: {
        headers: {
          /** @description GET path for the created log. */
          Location?: string;
          [name: string]: unknown;
        };
        content: {
          "application/json": components["schemas"]["FoodLog"];
        };
      };
      /** @description A field is missing or malformed; the message names it. */
      400: {
        headers: {
          [name: string]: unknown;
        };
        content: {
          "application/json": components["schemas"]["ApiError"];
        };
      };
      /**
       * @description The request carried no `Authorization` header, or the credential in it was rejected.
       *
       *     `unauthorized` — the header is missing or malformed, or the key is not one we recognise. A valid key belonging to the other API version is `403 forbidden` instead.
       *
       *     A client token is rejected in one of three ways, and only the first should be handled automatically:
       *
       *     - `token_expired` — the token is past its TTL. Mint a fresh one from your backend and retry the request once. This is routine and expected once per TTL window.
       *     - `token_invalid` — no such token: it was never issued, or it has been purged, which happens shortly after it expires. This code is not an automatic token-refresh signal.
       *     - `token_revoked` — the token was revoked, by `POST /v1.2/auth/client-token-revocations` or from the dashboard. The end user signs in again in your app, and your backend decides whether to mint another; a device that just re-mints defeats the revocation.
       */
      401: {
        headers: {
          /** @description RFC 6750 challenge. `Bearer` when the request carried no credential; `Bearer error="invalid_token", error_description="token expired|token invalid|token revoked"` when one was rejected. */
          "WWW-Authenticate"?: string;
          [name: string]: unknown;
        };
        content: {
          "application/json": components["schemas"]["ApiError"];
        };
      };
      /**
       * @description The credential is valid but is not allowed to make this request. `forbidden` is the general case — a key issued for the other API version, for example. A client token adds `client_token_not_allowed`, meaning the endpoint takes only an `sk-` API key — its description opens with **API key only.** (in this API: `POST /v1.2/auth/client-tokens`, `POST /v1.2/auth/client-token-revocations`, `POST /v1.2/chat/completions`, and `GET /v1.2/credits`).
       *
       *     On an endpoint that opens with **API key or client token.**, `scope_insufficient` means the token was minted without the scope named at the end of that endpoint’s description, and `end_user_id_mismatch` means the `January-End-User-ID` header disagrees with the end user the token is bound to — omit it, or send exactly that id.
       */
      403: {
        headers: {
          [name: string]: unknown;
        };
        content: {
          "application/json": components["schemas"]["ApiError"];
        };
      };
      /** @description Either a rate limit was exceeded (`code: rate_limited`) or the monthly credit allowance is spent (`code: credit_limit_exceeded`). When Retry-After is present, wait that many seconds; a per-day allowance resets 24 hours after the first request in its window. Credit exhaustion carries no Retry-After and retrying does not help — the allowance returns at the start of the next calendar month. Call `GET /v1.2/credits` for the balance and reset date. */
      429: {
        headers: {
          /** @description Seconds to wait before retrying; present when the window is known. */
          "Retry-After"?: string;
          [name: string]: unknown;
        };
        content: {
          "application/json": components["schemas"]["ApiError"];
        };
      };
      /** @description Any other error: the HTTP status plus { code, message }. Retry only rate_limited and the transient 5xx codes. */
      default: {
        headers: {
          [name: string]: unknown;
        };
        content: {
          "application/json": components["schemas"]["ApiError"];
        };
      };
    };
  };
  getFoodLog: {
    parameters: {
      query?: never;
      header?: {
        /**
         * @description Your stable ID for the end user whose food logs this request reads or writes. Opaque to January — use the same ID your system already uses for them.
         *
         *     | Credential | Header | Result |
         *     | --- | --- | --- |
         *     | API key (`sk-…`) | absent | `400 end_user_id_required` |
         *     | API key (`sk-…`) | present | the request acts on that end user |
         *     | Client token (`ct-…`) | absent | filled in from the token |
         *     | Client token (`ct-…`) | the end user the token is bound to | accepted |
         *     | Client token (`ct-…`) | any other end user | `403 end_user_id_mismatch` |
         */
        "January-End-User-ID"?: components["parameters"]["JanuaryEndUserId"];
      };
      path: {
        /** @description The log id returned when the log was created. */
        log_id: string;
      };
      cookie?: never;
    };
    requestBody?: never;
    responses: {
      /** @description The requested food log, hydrated with resolved food and nutrition. */
      200: {
        headers: {
          [name: string]: unknown;
        };
        content: {
          "application/json": components["schemas"]["FoodLog"];
        };
      };
      /** @description log_id is not a UUID. */
      400: {
        headers: {
          [name: string]: unknown;
        };
        content: {
          "application/json": components["schemas"]["ApiError"];
        };
      };
      /**
       * @description The request carried no `Authorization` header, or the credential in it was rejected.
       *
       *     `unauthorized` — the header is missing or malformed, or the key is not one we recognise. A valid key belonging to the other API version is `403 forbidden` instead.
       *
       *     A client token is rejected in one of three ways, and only the first should be handled automatically:
       *
       *     - `token_expired` — the token is past its TTL. Mint a fresh one from your backend and retry the request once. This is routine and expected once per TTL window.
       *     - `token_invalid` — no such token: it was never issued, or it has been purged, which happens shortly after it expires. This code is not an automatic token-refresh signal.
       *     - `token_revoked` — the token was revoked, by `POST /v1.2/auth/client-token-revocations` or from the dashboard. The end user signs in again in your app, and your backend decides whether to mint another; a device that just re-mints defeats the revocation.
       */
      401: {
        headers: {
          /** @description RFC 6750 challenge. `Bearer` when the request carried no credential; `Bearer error="invalid_token", error_description="token expired|token invalid|token revoked"` when one was rejected. */
          "WWW-Authenticate"?: string;
          [name: string]: unknown;
        };
        content: {
          "application/json": components["schemas"]["ApiError"];
        };
      };
      /**
       * @description The credential is valid but is not allowed to make this request. `forbidden` is the general case — a key issued for the other API version, for example. A client token adds `client_token_not_allowed`, meaning the endpoint takes only an `sk-` API key — its description opens with **API key only.** (in this API: `POST /v1.2/auth/client-tokens`, `POST /v1.2/auth/client-token-revocations`, `POST /v1.2/chat/completions`, and `GET /v1.2/credits`).
       *
       *     On an endpoint that opens with **API key or client token.**, `scope_insufficient` means the token was minted without the scope named at the end of that endpoint’s description, and `end_user_id_mismatch` means the `January-End-User-ID` header disagrees with the end user the token is bound to — omit it, or send exactly that id.
       */
      403: {
        headers: {
          [name: string]: unknown;
        };
        content: {
          "application/json": components["schemas"]["ApiError"];
        };
      };
      /** @description No log with this id for this user. */
      404: {
        headers: {
          [name: string]: unknown;
        };
        content: {
          "application/json": components["schemas"]["ApiError"];
        };
      };
      /** @description Either a rate limit was exceeded (`code: rate_limited`) or the monthly credit allowance is spent (`code: credit_limit_exceeded`). When Retry-After is present, wait that many seconds; a per-day allowance resets 24 hours after the first request in its window. Credit exhaustion carries no Retry-After and retrying does not help — the allowance returns at the start of the next calendar month. Call `GET /v1.2/credits` for the balance and reset date. */
      429: {
        headers: {
          /** @description Seconds to wait before retrying; present when the window is known. */
          "Retry-After"?: string;
          [name: string]: unknown;
        };
        content: {
          "application/json": components["schemas"]["ApiError"];
        };
      };
      /** @description Any other error: the HTTP status plus { code, message }. Retry only rate_limited and the transient 5xx codes. */
      default: {
        headers: {
          [name: string]: unknown;
        };
        content: {
          "application/json": components["schemas"]["ApiError"];
        };
      };
    };
  };
  deleteFoodLog: {
    parameters: {
      query?: never;
      header?: {
        /**
         * @description Your stable ID for the end user whose food logs this request reads or writes. Opaque to January — use the same ID your system already uses for them.
         *
         *     | Credential | Header | Result |
         *     | --- | --- | --- |
         *     | API key (`sk-…`) | absent | `400 end_user_id_required` |
         *     | API key (`sk-…`) | present | the request acts on that end user |
         *     | Client token (`ct-…`) | absent | filled in from the token |
         *     | Client token (`ct-…`) | the end user the token is bound to | accepted |
         *     | Client token (`ct-…`) | any other end user | `403 end_user_id_mismatch` |
         */
        "January-End-User-ID"?: components["parameters"]["JanuaryEndUserId"];
      };
      path: {
        /** @description The log id returned when the log was created. */
        log_id: string;
      };
      cookie?: never;
    };
    requestBody?: never;
    responses: {
      /** @description The log was deleted; empty body. Idempotent — deleting an already-deleted or unknown log also answers 204. */
      204: {
        headers: {
          [name: string]: unknown;
        };
        content?: never;
      };
      /** @description log_id is not a UUID. */
      400: {
        headers: {
          [name: string]: unknown;
        };
        content: {
          "application/json": components["schemas"]["ApiError"];
        };
      };
      /**
       * @description The request carried no `Authorization` header, or the credential in it was rejected.
       *
       *     `unauthorized` — the header is missing or malformed, or the key is not one we recognise. A valid key belonging to the other API version is `403 forbidden` instead.
       *
       *     A client token is rejected in one of three ways, and only the first should be handled automatically:
       *
       *     - `token_expired` — the token is past its TTL. Mint a fresh one from your backend and retry the request once. This is routine and expected once per TTL window.
       *     - `token_invalid` — no such token: it was never issued, or it has been purged, which happens shortly after it expires. This code is not an automatic token-refresh signal.
       *     - `token_revoked` — the token was revoked, by `POST /v1.2/auth/client-token-revocations` or from the dashboard. The end user signs in again in your app, and your backend decides whether to mint another; a device that just re-mints defeats the revocation.
       */
      401: {
        headers: {
          /** @description RFC 6750 challenge. `Bearer` when the request carried no credential; `Bearer error="invalid_token", error_description="token expired|token invalid|token revoked"` when one was rejected. */
          "WWW-Authenticate"?: string;
          [name: string]: unknown;
        };
        content: {
          "application/json": components["schemas"]["ApiError"];
        };
      };
      /**
       * @description The credential is valid but is not allowed to make this request. `forbidden` is the general case — a key issued for the other API version, for example. A client token adds `client_token_not_allowed`, meaning the endpoint takes only an `sk-` API key — its description opens with **API key only.** (in this API: `POST /v1.2/auth/client-tokens`, `POST /v1.2/auth/client-token-revocations`, `POST /v1.2/chat/completions`, and `GET /v1.2/credits`).
       *
       *     On an endpoint that opens with **API key or client token.**, `scope_insufficient` means the token was minted without the scope named at the end of that endpoint’s description, and `end_user_id_mismatch` means the `January-End-User-ID` header disagrees with the end user the token is bound to — omit it, or send exactly that id.
       */
      403: {
        headers: {
          [name: string]: unknown;
        };
        content: {
          "application/json": components["schemas"]["ApiError"];
        };
      };
      /** @description Either a rate limit was exceeded (`code: rate_limited`) or the monthly credit allowance is spent (`code: credit_limit_exceeded`). When Retry-After is present, wait that many seconds; a per-day allowance resets 24 hours after the first request in its window. Credit exhaustion carries no Retry-After and retrying does not help — the allowance returns at the start of the next calendar month. Call `GET /v1.2/credits` for the balance and reset date. */
      429: {
        headers: {
          /** @description Seconds to wait before retrying; present when the window is known. */
          "Retry-After"?: string;
          [name: string]: unknown;
        };
        content: {
          "application/json": components["schemas"]["ApiError"];
        };
      };
      /** @description Any other error: the HTTP status plus { code, message }. Retry only rate_limited and the transient 5xx codes. */
      default: {
        headers: {
          [name: string]: unknown;
        };
        content: {
          "application/json": components["schemas"]["ApiError"];
        };
      };
    };
  };
  updateFoodLog: {
    parameters: {
      query?: never;
      header?: {
        /**
         * @description Your stable ID for the end user whose food logs this request reads or writes. Opaque to January — use the same ID your system already uses for them.
         *
         *     | Credential | Header | Result |
         *     | --- | --- | --- |
         *     | API key (`sk-…`) | absent | `400 end_user_id_required` |
         *     | API key (`sk-…`) | present | the request acts on that end user |
         *     | Client token (`ct-…`) | absent | filled in from the token |
         *     | Client token (`ct-…`) | the end user the token is bound to | accepted |
         *     | Client token (`ct-…`) | any other end user | `403 end_user_id_mismatch` |
         */
        "January-End-User-ID"?: components["parameters"]["JanuaryEndUserId"];
      };
      path: {
        /** @description The log id returned when the log was created. */
        log_id: string;
      };
      cookie?: never;
    };
    requestBody: {
      content: {
        "application/json": components["schemas"]["UpdateFoodLogRequest"];
      };
    };
    responses: {
      /** @description The updated log, hydrated with resolved food and nutrition, reflecting the applied changes. */
      200: {
        headers: {
          [name: string]: unknown;
        };
        content: {
          "application/json": components["schemas"]["FoodLog"];
        };
      };
      /** @description A field is malformed; the message names it. */
      400: {
        headers: {
          [name: string]: unknown;
        };
        content: {
          "application/json": components["schemas"]["ApiError"];
        };
      };
      /**
       * @description The request carried no `Authorization` header, or the credential in it was rejected.
       *
       *     `unauthorized` — the header is missing or malformed, or the key is not one we recognise. A valid key belonging to the other API version is `403 forbidden` instead.
       *
       *     A client token is rejected in one of three ways, and only the first should be handled automatically:
       *
       *     - `token_expired` — the token is past its TTL. Mint a fresh one from your backend and retry the request once. This is routine and expected once per TTL window.
       *     - `token_invalid` — no such token: it was never issued, or it has been purged, which happens shortly after it expires. This code is not an automatic token-refresh signal.
       *     - `token_revoked` — the token was revoked, by `POST /v1.2/auth/client-token-revocations` or from the dashboard. The end user signs in again in your app, and your backend decides whether to mint another; a device that just re-mints defeats the revocation.
       */
      401: {
        headers: {
          /** @description RFC 6750 challenge. `Bearer` when the request carried no credential; `Bearer error="invalid_token", error_description="token expired|token invalid|token revoked"` when one was rejected. */
          "WWW-Authenticate"?: string;
          [name: string]: unknown;
        };
        content: {
          "application/json": components["schemas"]["ApiError"];
        };
      };
      /**
       * @description The credential is valid but is not allowed to make this request. `forbidden` is the general case — a key issued for the other API version, for example. A client token adds `client_token_not_allowed`, meaning the endpoint takes only an `sk-` API key — its description opens with **API key only.** (in this API: `POST /v1.2/auth/client-tokens`, `POST /v1.2/auth/client-token-revocations`, `POST /v1.2/chat/completions`, and `GET /v1.2/credits`).
       *
       *     On an endpoint that opens with **API key or client token.**, `scope_insufficient` means the token was minted without the scope named at the end of that endpoint’s description, and `end_user_id_mismatch` means the `January-End-User-ID` header disagrees with the end user the token is bound to — omit it, or send exactly that id.
       */
      403: {
        headers: {
          [name: string]: unknown;
        };
        content: {
          "application/json": components["schemas"]["ApiError"];
        };
      };
      /** @description No log with this id for this user. */
      404: {
        headers: {
          [name: string]: unknown;
        };
        content: {
          "application/json": components["schemas"]["ApiError"];
        };
      };
      /** @description Either a rate limit was exceeded (`code: rate_limited`) or the monthly credit allowance is spent (`code: credit_limit_exceeded`). When Retry-After is present, wait that many seconds; a per-day allowance resets 24 hours after the first request in its window. Credit exhaustion carries no Retry-After and retrying does not help — the allowance returns at the start of the next calendar month. Call `GET /v1.2/credits` for the balance and reset date. */
      429: {
        headers: {
          /** @description Seconds to wait before retrying; present when the window is known. */
          "Retry-After"?: string;
          [name: string]: unknown;
        };
        content: {
          "application/json": components["schemas"]["ApiError"];
        };
      };
      /** @description Any other error: the HTTP status plus { code, message }. Retry only rate_limited and the transient 5xx codes. */
      default: {
        headers: {
          [name: string]: unknown;
        };
        content: {
          "application/json": components["schemas"]["ApiError"];
        };
      };
    };
  };
  predictGlucoseResponse: {
    parameters: {
      query?: never;
      header?: never;
      path?: never;
      cookie?: never;
    };
    requestBody: {
      content: {
        "application/json": components["schemas"]["GlucosePredictionRequest"];
      };
    };
    responses: {
      /** @description `points`, the predicted glucose curve at 15-minute intervals, plus the meal's overall impact score and suggested chart bounds for rendering it. */
      200: {
        headers: {
          [name: string]: unknown;
        };
        content: {
          "application/json": components["schemas"]["GlucosePrediction"];
        };
      };
      /** @description A field is missing or invalid; the message names it and the accepted values. */
      400: {
        headers: {
          [name: string]: unknown;
        };
        content: {
          "application/json": components["schemas"]["ApiError"];
        };
      };
      /**
       * @description The request carried no `Authorization` header, or the credential in it was rejected.
       *
       *     `unauthorized` — the header is missing or malformed, or the key is not one we recognise. A valid key belonging to the other API version is `403 forbidden` instead.
       *
       *     A client token is rejected in one of three ways, and only the first should be handled automatically:
       *
       *     - `token_expired` — the token is past its TTL. Mint a fresh one from your backend and retry the request once. This is routine and expected once per TTL window.
       *     - `token_invalid` — no such token: it was never issued, or it has been purged, which happens shortly after it expires. This code is not an automatic token-refresh signal.
       *     - `token_revoked` — the token was revoked, by `POST /v1.2/auth/client-token-revocations` or from the dashboard. The end user signs in again in your app, and your backend decides whether to mint another; a device that just re-mints defeats the revocation.
       */
      401: {
        headers: {
          /** @description RFC 6750 challenge. `Bearer` when the request carried no credential; `Bearer error="invalid_token", error_description="token expired|token invalid|token revoked"` when one was rejected. */
          "WWW-Authenticate"?: string;
          [name: string]: unknown;
        };
        content: {
          "application/json": components["schemas"]["ApiError"];
        };
      };
      /**
       * @description The credential is valid but is not allowed to make this request. `forbidden` is the general case — a key issued for the other API version, for example. A client token adds `client_token_not_allowed`, meaning the endpoint takes only an `sk-` API key — its description opens with **API key only.** (in this API: `POST /v1.2/auth/client-tokens`, `POST /v1.2/auth/client-token-revocations`, `POST /v1.2/chat/completions`, and `GET /v1.2/credits`).
       *
       *     On an endpoint that opens with **API key or client token.**, `scope_insufficient` means the token was minted without the scope named at the end of that endpoint’s description.
       */
      403: {
        headers: {
          [name: string]: unknown;
        };
        content: {
          "application/json": components["schemas"]["ApiError"];
        };
      };
      /** @description Either a rate limit was exceeded (`code: rate_limited`) or the monthly credit allowance is spent (`code: credit_limit_exceeded`). When Retry-After is present, wait that many seconds; a per-day allowance resets 24 hours after the first request in its window. Credit exhaustion carries no Retry-After and retrying does not help — the allowance returns at the start of the next calendar month. Call `GET /v1.2/credits` for the balance and reset date. */
      429: {
        headers: {
          /** @description Seconds to wait before retrying; present when the window is known. */
          "Retry-After"?: string;
          [name: string]: unknown;
        };
        content: {
          "application/json": components["schemas"]["ApiError"];
        };
      };
      /** @description The prediction took too long; retry. */
      504: {
        headers: {
          [name: string]: unknown;
        };
        content: {
          "application/json": components["schemas"]["ApiError"];
        };
      };
      /** @description Any other error: the HTTP status plus { code, message }. Retry only rate_limited and the transient 5xx codes. */
      default: {
        headers: {
          [name: string]: unknown;
        };
        content: {
          "application/json": components["schemas"]["ApiError"];
        };
      };
    };
  };
}
